---
name: cogover-custom-module
description: "Điều phối vòng đời Cogover Custom Module: khảo sát App/Object, chọn backend/frontend và dạng frontend (single page app, custom component, Federation Page), người dùng xác nhận trước khi code, schema duyệt bằng Excel, security rules, quy tắc nghiệp vụ theo Super Admin/role của người gọi, record trigger before/after-change, background job (enqueue, lịch cron, retry) hoặc Process, push message (làm mới record, toast), gửi notification và email từ hộp thư được grant, đọc cơ cấu tổ chức (phòng ban, vị trí, chuỗi quản lý người duyệt), secret/credential cho fetch, crypto (SHA-256, HMAC, mã hoá AES/RSA, chữ ký RSA/ECDSA), inbound webhook (KEY/HMAC), Project/policy/key, code/test theo template, publish qua Dev CLI, bàn giao. Không thay skill cấu hình Object/Process đơn lẻ."
metadata:
  author: cogover
  version: "1.11.0"
---

# Cogover Custom Module

- **Phiên bản:** `1.11.0`
- **Ngày phát hành:** `2026-09-25`

## Tổng quan và phạm vi

Skill điều phối toàn bộ vòng đời Custom Module. Dùng `$cogover-overview` làm kiến thức nền; không chuyển vòng về orchestrator khác khi yêu cầu đã thuộc Custom Module. Chỉ dùng public API, metadata Workspace và tài liệu sản phẩm để xác định khả năng; không yêu cầu mã nguồn/hạ tầng của Cogover.

Custom Module dành cho nghiệp vụ mà Cogover Object/Process chuẩn chưa đáp ứng đủ: logic phía server, giao diện riêng hoặc cả hai, vẫn dùng dữ liệu và quyền của Workspace.

| Loại | Chọn khi | Ví dụ |
|---|---|---|
| Custom Frontend Module | Cần màn hình hoặc thành phần giao diện riêng; API hiện có đã đủ dữ liệu và logic với quyền người dùng. Ba dạng: single page app, custom component nhúng layout Object, Federation Page trong ứng dụng Cogover | Dashboard toàn màn hình, khối kiểm tra mã giảm giá trên form đơn hàng, trang công cụ trong menu ứng dụng |
| Custom Backend Module | Cần xử lý phía server, tổng hợp/ghi Object, API riêng, tích hợp có thông tin bí mật do Workspace giữ (secret/credential), quy tắc kiểm tra/điều chỉnh record trước khi lưu và xử lý tiếp sau khi lưu áp cho mọi nguồn ghi (record trigger), việc nền dài hoặc chạy theo lịch cron có retry (background job), nhận webhook từ hệ thống ngoài không có phiên người dùng (inbound webhook), ký/kiểm tra chữ ký, mã hoá/giải mã dữ liệu trao đổi với đối tác (crypto), làm mới record/toast cho người đang mở record (push message), gửi notification vào danh sách thông báo hoặc email từ hộp thư của Workspace (notification, email), hoặc dựa vào phòng ban, vị trí và chuỗi quản lý để chọn người duyệt, kiểm tra quyền duyệt (cơ cấu tổ chức) | Tính giá, đồng bộ ERP hằng đêm, API tổng hợp tồn kho, chặn xoá đơn đã hoàn thành, nhận sự kiện thanh toán từ cổng thanh toán, ký và mã hoá request gửi ngân hàng, tính lại tổng đơn rồi tải lại trang record đang mở, báo quản lý trực tiếp khi có yêu cầu nghỉ phép, chỉ cho quản lý của người tạo duyệt đề nghị thanh toán, gửi báo giá kèm file từ hộp thư dùng chung |
| Cả hai | Màn hình riêng cần gọi logic riêng phía backend | Màn hình xử lý đơn hàng gọi API tính giá và cập nhật đơn |

Mã frontend được tải xuống trình duyệt: logic nhạy cảm, quyền nâng cao và credential tích hợp phải nằm ở backend.

Ba dạng frontend khác nhau về bộ khung code và nơi hiển thị: single page app dùng stack tự chọn và mở qua `/{slugSlot}/index.html`; custom component và Federation Page bắt buộc dùng template React `custom-frontend-module-template` của Cogover, lần lượt nhúng vào layout Object qua item Federation component và hiển thị tại `/{APP_SLUG}/c{N}/{PATH}`. Chọn theo [Các loại Custom Module và dạng frontend](references/get-started-custom-module.md); lựa chọn phải được người dùng xác nhận theo mục 1A trước khi phát triển.

Môi trường cần sub-agent, shell/HTTP, công cụ build/test và trình duyệt khi làm frontend. Không có sub-agent: hoàn tất phần tư vấn/chuẩn bị làm được độc lập, nêu rõ điều kiện còn thiếu trước các bước bắt buộc giao agent, không tuyên bố đã đáp ứng yêu cầu phân công và không tự nhận vai reviewer để giả lập đánh giá độc lập.

## Đầu vào

- `WORKSPACE_DOMAIN`: hostname đầy đủ hoặc HTTPS origin. Chấp nhận tên viết nhầm `WORKSAPCE_DOMAIN` và chuẩn hóa về `WORKSPACE_DOMAIN`; không tự nối domain suffix, không giữ path/query trong origin.
- Workspace API Key. Credential, header và quy ước response/lỗi chung: theo [$cogover-api-auth](../cogover-api-auth/SKILL.md). Skill dùng cả `/bapi/v1` (API Key Bearer: tạo phiên, metadata) lẫn `/api/v1` (phiên Web App: App, quản lý Project, gọi module); ngoại lệ riêng: mọi request `/api/v1/ts-projects/...` gửi cả `x-req-type: 9` lẫn `x-req-service` (`3` production, `4` quản lý, `6` preview), thiếu `x-req-type: 9` thì service `4` bị Authorization Server xử lý như logout và thu hồi phiên; quản lý TS Project dùng `POST` cho cả đọc/list và trả object trực tiếp, không bắt buộc envelope `r: 0`; với `x-req-type: 9` response là nguyên status/header/body của Runtime, không bọc `{serviceVersion, service, id, type, body}`, lỗi do Authorization Server sinh ra có header `x-proxy-error: 1`. Cogover Dev CLI đọc key theo tên `COGOVER_API_KEY` với thứ tự nguồn credential riêng: [CLI, session và bàn giao](references/cli-session-and-delivery.md).
- Yêu cầu người dùng: luồng nghiệp vụ, người sử dụng, dữ liệu vào/ra, thao tác đọc/ghi, UI, trigger/lịch/tích hợp, kết quả mong đợi. Chỉ làm rõ phần chưa biết có ảnh hưởng thiết kế.
- Sửa dự án: tra project ID/slug, thư mục source và version đang active; resolve đúng project trước khi sửa.
- Không ghi key/secret vào source, `cogover.json`, frontend bundle, archive, log hay báo cáo. Không hardcode credential tích hợp kể cả trong source backend; chỉ dùng secret/credential của Project (bước 6A) và đọc qua `fetch({ credential })`, `secrets.get` hoặc key `{ secret: name }` của thao tác `crypto` (HMAC, AES, RSA, `sign`); AES key và private key luôn lưu thành secret, không viết trong source. Giá trị secret, inbound key và HMAC secret do người dùng/quản trị viên nhập qua prompt ẩn hoặc stdin của CLI, không qua chat. Backend không mặc nhiên có mọi API của Node.js.

## Tài liệu theo nhánh

Đọc bản hiện tại của mỗi skill được gọi và reference bắt buộc trước khi thao tác; truyền lại Workspace và yêu cầu đã xác định, không hỏi lặp đầu vào.

| Khi thực hiện | Đọc |
|---|---|
| Chọn loại module và dạng frontend, chuẩn bị xác nhận 1A | [Các loại Custom Module và dạng frontend](references/get-started-custom-module.md) |
| Xác thực, quản lý Project hoặc bàn giao cURL | [$cogover-api-auth](../cogover-api-auth/SKILL.md) và [CLI, session và bàn giao](references/cli-session-and-delivery.md) |
| Thiết kế quyền, tạo security rules cho Object mới | [$user-permission](../user-permission/SKILL.md) và [Object Security Rules API](../user-permission/references/api-object-security-rules.md), gồm rule giữ chỗ không chọn nhân sự |
| Quản lý backend, policy, key, version, production/preview | [Custom Backend Module API Reference](references/custom-backend-module-api-reference.md) |
| Quản lý frontend, version và asset URL | [Custom Frontend Module API Reference](references/custom-frontend-module-api-reference.md) |
| Viết hoặc sửa code backend, chọn API đặc biệt | Bảng [Chọn capability backend](#chọn-capability-backend) bên dưới, rồi [SDK usage guide](references/cogover-sdk-usage-guide.md) đọc trước khi code (mẫu handler, filter, fetch, secret/credential, state, lock, push message, background job, notification, email, cơ cấu tổ chức, danh tính, quy tắc theo role người gọi, TypeScript config, router, nhận webhook), rồi [SDK API reference](references/cogover-sdk-api-reference.md): nền tảng handler/router/request/response, errors và các mục capability đang dùng, gồm [Push message](references/cogover-sdk-api-reference.md#push-message) khi cần làm mới record, toast hoặc message ngầm cho web client |
| Cần ví dụ chạy được cho một API SDK cụ thể | Repo [cogover-sdk-samples](https://github.com/cogover/cogover-sdk-samples): mỗi file `src/samples/<area>/<name>.ts` là một route minh họa một nhóm API (router, response, invocation, records, filters, identity, state, locks, fetch, schema, logging, errors, push, jobs, secrets, crypto, inbound webhook, cơ cấu tổ chức `org`, notification, email), `src/triggers/` và `src/jobs/` là trigger/job mẫu, `GET /` là catalog kèm cURL; README (Anh/Việt) có bảng tra API → file. Đọc file sample tương ứng trước khi code một API chưa quen; API chưa có dòng trong bảng của README thì theo usage guide và quick start, không suy đoán từ sample khác; kiểm tra phiên bản `@cogover/sdk` ghim trong `package.json` của repo khớp bản dự án (từ `0.12.0` mới có sample `org`, notification, email); sample notification/email gửi tới người thật nên chỉ chạy trên Workspace thử nghiệm, cần notification channel của Workspace và mục `email` trong identity policy theo README của repo; không sao chép `cogover.json`, Object mẫu `sample_*`, sample ghi/xoá record hay lịch mẫu vào project nghiệp vụ |
| Khởi tạo/code/test backend local | [Backend quick start](references/get-started-custom-backend-module.md) |
| Viết record trigger before-change/after-change, chạy trigger local, publish/activate và kiểm thử bằng thay đổi bản ghi thật | [Record trigger quick start](references/get-started-record-trigger.md), mục Record trigger trong [SDK usage guide](references/cogover-sdk-usage-guide.md#record-trigger) và [SDK API reference](references/cogover-sdk-api-reference.md#record-trigger) |
| Việc nền dài, chạy trễ, chạy theo lịch cron, retry lỗi tạm thời, enqueue từ route/trigger/webhook | [Background job quick start](references/get-started-background-jobs.md), mục Chạy việc nền bằng job trong [SDK usage guide](references/cogover-sdk-usage-guide.md#chạy-việc-nền-bằng-job) và [Background job](references/cogover-sdk-api-reference.md#background-job) trong SDK API reference; xem run/lịch và enqueue thủ công theo mục Background job của [Backend API Reference](references/custom-backend-module-api-reference.md#background-job) hoặc `cogover-dev jobs` |
| Gọi API ngoài có token, lưu/xoay secret, ký bằng secret | [Secrets và credentials quick start](references/get-started-secrets-and-credentials.md), mục Dùng secret và credential trong [SDK usage guide](references/cogover-sdk-usage-guide.md#dùng-secret-và-credential), [Secret và credential](references/cogover-sdk-api-reference.md#secret-và-credential) và [Xác thực bằng credential](references/cogover-sdk-api-reference.md#xác-thực-bằng-credential) trong SDK API reference; quản lý theo mục Secret của [Backend API Reference](references/custom-backend-module-api-reference.md#secret) hoặc `cogover-dev secrets` |
| Băm, ký/kiểm tra HMAC, token ngẫu nhiên, so sánh an toàn; mã hoá/giải mã AES hoặc RSA, ký/xác minh RSA hoặc ECDSA (API ngân hàng, JWT) | [Crypto quick start](references/get-started-crypto.md), mục Mã hoá, giải mã và ký dữ liệu trong [SDK usage guide](references/cogover-sdk-usage-guide.md#mã-hoá-giải-mã-và-ký-dữ-liệu) và [Mã hoá và chữ ký](references/cogover-sdk-api-reference.md#mã-hoá-và-chữ-ký) trong SDK API reference (định dạng key ở mục [Key](references/cogover-sdk-api-reference.md#key), bảng JWT `alg` ở mục `crypto.sign`) |
| Nhận webhook từ hệ thống ngoài không có phiên Cogover | [Inbound webhook quick start](references/get-started-inbound-webhooks.md), mục Nhận webhook trong [SDK usage guide](references/cogover-sdk-usage-guide.md#nhận-webhook) và [Inbound webhook](references/cogover-sdk-api-reference.md#inbound-webhook) trong SDK API reference; tạo/rotate/revoke inbound access và cách bên gửi gọi theo mục Inbound access và [Gọi inbound webhook](references/custom-backend-module-api-reference.md#gọi-inbound-webhook) của Backend API Reference hoặc `cogover-dev inbound` |
| Gửi notification (danh sách thông báo, web/mobile push, bản sao email theo notification channel) hoặc email từ hộp thư dùng chung/hộp thư cá nhân của actor, ghi nhận email trên timeline record | Mục Gửi notification và email trong [SDK usage guide](references/cogover-sdk-usage-guide.md#gửi-notification-và-email), [Notification](references/cogover-sdk-api-reference.md#notification) và [Email](references/cogover-sdk-api-reference.md#email) trong SDK API reference; grant hộp thư gửi theo mục [Người gửi email](references/custom-backend-module-api-reference.md#người-gửi-email) của Backend API Reference; notification channel là record của Object `notification_channel`, resolve record ID bằng [$object-record](../object-record/SKILL.md) và ý nghĩa kênh theo [Thông báo ở Tầng 2](../cogover-overview/references/tang2-business-logic.md#thông-báo) |
| Đọc phòng ban, vị trí, nhân sự của người gọi; chuỗi quản lý, chọn người duyệt, kiểm tra quyền duyệt hoặc thuộc phòng ban | [Cơ cấu tổ chức quick start](references/get-started-organization-structure.md), mục Đọc phòng ban, vị trí và quản lý trong [SDK usage guide](references/cogover-sdk-usage-guide.md#đọc-phòng-ban-vị-trí-và-quản-lý) và [Cơ cấu tổ chức](references/cogover-sdk-api-reference.md#cơ-cấu-tổ-chức) trong SDK API reference; resolve hoặc chuẩn bị dữ liệu phòng ban, nhân sự, vị trí, cấp quản lý (`level`) và phòng ban chính (`is_primary`) để thiết kế và kiểm thử bằng [$user-permission](../user-permission/SKILL.md) (Departments, Personnels, Positions API) |
| Khởi tạo/build/test frontend single page app | [Frontend quick start](references/get-started-custom-frontend-module.md) |
| Khởi tạo/code/test custom component nhúng layout | [Custom component quick start](references/get-started-custom-component.md) cùng `README.vi.md`, `.agents/skills/custom-module-foundation`, `.agents/skills/custom-module-form-builder` và `src/types/form-builder.d.ts` trong template đã clone |
| Khởi tạo/code/test Federation Page | [Federation Page quick start](references/get-started-federation-page.md) cùng `README.vi.md`, `.agents/skills/custom-module-foundation` và `.agents/skills/custom-module-api` trong template đã clone |
| Frontend gọi backend | [Full-stack integration](references/full-stack-integration.md) |
| Preview/apply, ghi hàng loạt, decimal hoặc concurrency | [Batch writes và checkpoint](references/batch-writes-and-checkpoints.md), cùng contract state/locks/records trong SDK |

Hai API reference là nguồn chuẩn cho HTTP contract; quick start chỉ là cách làm với CLI và sample. Tất cả là snapshot tài liệu sản phẩm tiếng Việt ngày `2026-09-06`, riêng ba tài liệu về các dạng frontend, custom component và Federation Page ngày `2026-09-15`, Backend quick start và Record trigger quick start ngày `2026-09-21`, Secrets/credentials quick start ngày `2026-09-22`, Crypto quick start ngày `2026-09-23`, Backend API Reference cùng ba quick start background job, inbound webhook và cơ cấu tổ chức ngày `2026-09-24`; SDK usage guide và SDK API reference theo `@cogover/sdk` `0.12.0` (snapshot ngày `2026-09-24`; record trigger và `records.getMany` cần SDK từ `0.6.0`, push message từ `0.7.0`, background job, secret/credential, crypto hash/HMAC/random và inbound webhook từ `0.8.0`, mã hoá AES/RSA và chữ ký RSA/ECDSA (`aesEncrypt`, `aesDecrypt`, `rsaEncrypt`, `rsaDecrypt`, `sign`, `verify`) từ `0.9.0`, cờ Super Admin và danh sách role của người gọi (`invocation.user.membership.isSuperAdmin`, `roles`) từ `0.10.0`, cơ cấu tổ chức (`org`) từ `0.11.0`, notification và email (`notifications.send`, `email.send`, `email.senders`) từ `0.12.0`; chạy trigger local cần Dev CLI từ `0.10.0`, các lệnh `secrets`, `inbound`, `jobs` cần Dev CLI từ `0.13.1`, truyền `isSuperAdmin`/`roles` vào script local và đọc `org` trên local cần Dev CLI từ `0.14.0`, thử `notifications.send`/`email.send` trên local cần Dev CLI từ `0.15.0`, bản xếp hai thao tác này là ghi nên session `--allow-writes=false` từ chối), SDK cài đặt khác phiên bản thì đối chiếu thêm tài liệu public đi kèm package. Với custom component và Federation Page, các skill trong `.agents/skills` của template đã clone là nguồn chuẩn cho quy ước code (expose, routing, `Link`, asset, API client, i18n); skill này quyết định workflow, phê duyệt, kiểm thử và publish. Template thay đổi thì theo bản đã clone, không theo trích dẫn trong quick start. ID, field slug, policy và dữ liệu mẫu chỉ minh họa, không thay dữ liệu thật của Workspace. Không suy ra API từ tên gọi hoặc dùng API không được hỗ trợ.

## Chọn capability backend

Chọn theo nhu cầu thực tế của contract ở bước 1 mục 5; mỗi capability có giới hạn riêng trong SDK API reference. Mọi capability ngoài route đều là một phần của version backend (áp dụng khi activate) trừ secret/credential và inbound access được cấu hình trên Project và dùng ngay cho version active; grant hộp thư gửi email nằm trong identity policy nên phải duyệt cho từng version.

| Nhu cầu | Dùng | Lưu ý, không dùng khi |
|---|---|---|
| Trả kết quả ngay trong HTTP response cho người dùng hoặc frontend | HTTP route (`createRouter`) hoặc `defineScript` một endpoint | Việc quá vài giây, phải sống sót qua lỗi tạm thời hoặc restart: route chỉ kiểm tra input rồi `jobs.enqueue` và trả `202` với `runId` |
| Chỉ Super Admin hoặc người giữ role nhất định được thực hiện thao tác nghiệp vụ (duyệt, mở khoá, điều chỉnh) | `invocation.user.membership.isSuperAdmin` và `invocation.user.membership.roles` (`{ id, name }`, sắp theo tên); SDK từ `0.10.0` | Chỉ là quy tắc nghiệp vụ trong code: không thay security rules, quyền record/field vẫn theo danh tính người gọi và không có danh sách quyền của role; so theo role `id` (tên đổi được, ID khác nhau giữa Workspace); `user` là `null` với danh tính `system`/`inbound`; record trigger và background job có thể nhận thay đổi role chậm khoảng một phút |
| Quy tắc bắt buộc trước khi record được lưu, áp cho mọi nguồn ghi | Record trigger before-change | Chỉ đọc, fail-closed, ≤ 2 giây; không ghi record, `fetch`, state, lock, push, notification, email (kể cả `email.senders()`), `jobs.enqueue`, `secrets.get` hay thao tác `crypto` bằng key `{ secret }`; đọc `org` được |
| Xử lý tiếp ngay sau khi record được lưu | Record trigger after-change | Best-effort, có thể chạy lặp; việc dài hơn `timeoutMs` hoặc cần state/locks thì enqueue background job |
| Việc nền dài chia trang, gọi hệ thống ngoài có retry, chạy trễ (`delayMs`/`runAt` tối đa 30 ngày), chạy theo lịch cron | Background job (`defineJob`, named export `jobs`, `jobs.enqueue`, `schedule` cron năm trường với múi giờ IANA) | Workspace phải bật job; mỗi lần thử ≤ 60 giây, ít nhất một lần nên handler idempotent; run theo lịch, run do quản trị viên/CLI enqueue và run từ webhook chạy danh tính system (policy `allowInternalSystem`); không dùng cho luồng có người tham gia (chờ phê duyệt bằng User Task), AI Agent (Process) |
| Gọi API ngoài cần token trong HTTP header | `fetch(url, { credential })` với credential loại BEARER/BASIC/HEADER và host được phép | Code không thấy giá trị; request tới host ngoài danh sách, tự đặt cùng header hoặc tên không active bị `FETCH_BLOCKED`; không có trong before-change |
| Cần chính giá trị bí mật: ký, đặt vào body hay định dạng khác | `secrets.get` (secret OPAQUE) hoặc truyền `{ secret: name }` làm key của `crypto` (HMAC, AES, RSA, `sign`/`verify`) để ký, mã hoá, giải mã mà không đọc giá trị | ≤ 20 thao tác secret mỗi invocation, giá trị cache trong invocation; không ghi vào log/record/state/response; Development Session mặc định bị từ chối (`SECRETS_NOT_ALLOWED`); không có trong before-change |
| Băm nội dung, ký/kiểm tra HMAC, token và nonce ngẫu nhiên, so sánh chữ ký | `crypto.sha256`, `hmacSha256`, `randomBytes`, `randomUUID`, `timingSafeEqual` | Chỉ SHA-256; hash không che được giá trị dễ đoán và không dùng lưu mật khẩu, cần giữ bí mật thì mã hoá (dòng dưới); so sánh chữ ký/token luôn bằng `timingSafeEqual`; ký/kiểm tra trên đúng chuỗi byte gửi đi (`request.rawBody`); dùng được cả trong before-change trừ key `{ secret }` |
| Mã hoá/giải mã dữ liệu trao đổi với hệ thống ngoài; ký hoặc xác minh chữ ký khoá công khai (API ngân hàng, JWT `RS*`/`PS*`/`ES*`) | `crypto.aesEncrypt`/`aesDecrypt` (AES-GCM mặc định, CBC), `rsaEncrypt`/`rsaDecrypt` (OAEP-SHA256 mặc định, OAEP-SHA1, PKCS1), `sign`/`verify` (RSA PKCS #1 v1.5, RSA-PSS, ECDSA) với key `{ secret: name }`; SDK từ `0.9.0` | AES key và private key luôn là secret OPAQUE, không viết trong source; mode/padding/IV/tag/định dạng chữ ký/encoding phải khớp tài liệu bên kia; RSA chỉ mã hoá giá trị nhỏ (190 byte với key 2048 bit, OAEP-SHA256), dữ liệu lớn mã hoá bằng AES rồi chỉ mã hoá AES key bằng RSA; giải mã thất bại ném `DECRYPTION_FAILED`, `verify` trả `false` thay vì ném; key từ secret không có trong before-change |
| Hệ thống ngoài gọi vào không có phiên người dùng Cogover | Inbound webhook: route dưới `/hooks/` + inbound access KEY hoặc HMAC do quản trị viên tạo (bước 6A) | Cogover xác thực trước handler (`401` khi sai); handler kiểm tra `invocation.identity === "inbound"`, giữ ngắn, chống trùng theo event ID rồi enqueue job; record dùng danh tính system (policy); Workspace session không thay được inbound credential |
| Làm mới record đang mở, toast, message ngầm cho custom component | Push message (`push.refreshRecords`, `push.toast`, `push.message`) | Best-effort, không thu hồi; không có trong before-change và Development Session chỉ đọc; chỉ tới người đang mở web client, không lưu lại: việc người nhận cần xử lý sau thì dùng notification |
| Báo cho người dùng một việc cần xử lý sau (yêu cầu duyệt, nhắc hạn, kết quả xử lý), lưu trong danh sách thông báo và gửi qua web push, mobile push, bản sao email theo kênh | `notifications.send` (`to` 1–200 personnel ID, `exclude: ["actor"]`, `channel` là record ID của `notification_channel`, `link`, `sender`, `email: false`, `idempotencyKey`); SDK từ `0.12.0` | Không cần grant trong identity policy; best-effort, không thu hồi; tối đa 5.000 người nhận mỗi giờ mỗi project; personnel chưa có tài khoản bị bỏ qua (`skippedPersonnelIds`); bản sao email gửi từ địa chỉ thông báo của Cogover và tính vào hạn mức email thông báo hằng ngày theo gói; không có trong before-change, preview và Development Session chỉ đọc |
| Gửi email thật từ hộp thư của Workspace tới khách hàng hoặc nhân sự, ghi nhận trên timeline record, đính kèm file của record | `email.send` với `from: { mailbox: id }` (hộp thư dùng chung) hoặc `from: "actor"` (hộp thư cá nhân của người khởi đầu execution), `record`, `recordEmailFields`, `attachments`, `idempotencyKey`; `email.senders()` chọn hộp thư theo địa chỉ; SDK từ `0.12.0` | Hộp thư phải được grant trong mục `email` của identity policy đã duyệt cho đúng version (`workspaceMailboxIds`, `allowActorMailbox`), nếu không ném `EMAIL_SENDER_NOT_GRANTED`; execution không có người dùng cần `allowInternalSystem: true` và không dùng được `"actor"`; tối đa 50 người nhận, 10 file/20 MB, 500 email mỗi giờ mỗi project; lời gọi xong khi Cogover tiếp nhận, lỗi gửi sau đó không báo lại; không có trong before-change, `email.send` bị từ chối trong preview và Development Session chỉ đọc |
| Biết người gọi thuộc phòng ban/vị trí nào, liệt kê thành viên, tìm quản lý hoặc chuỗi người duyệt, kiểm tra ai quản lý ai hoặc ai thuộc phòng ban | `org` (`me`, `personnel.get`/`getMany`/`managerChain`, `departments.get`/`tree`/`ancestors`/`members`/`managers`/`managerChain`, `positions.list`/`get`/`members`, `isManagerOf`, `isInDepartment`); SDK từ `0.11.0` | Chỉ đọc, dùng được cả trong before-change; cấu trúc lấy từ bản cơ cấu đã cache, thay đổi có hiệu lực sau vài giây; `withDisplay: true` mới có tên, email, avatar (thêm lượt đọc, trang tối đa 200); field khác của nhân sự đọc qua `data.object("personnel")`; job, inbound webhook và trigger do hệ thống ghi cần `allowInternalSystem: true` (`IDENTITY_NOT_GRANTED`); `level` của membership là cấp quản lý (`0` nhân viên, `1` cao nhất), không phải độ sâu cây |
| Lưu tiến độ, checkpoint nhỏ; chặn chạy đồng thời | Project state (CAS `expectedVersion`) và distributed locks | Không transaction, không exactly-once; không lưu secret vào state; không có trong trigger context |

## Workflow

### Điều phối sub-agent

Agent chính giữ yêu cầu, quyết định kiến trúc, schema đã duyệt, contract tích hợp và nghiệm thu cuối; tự làm bước 4–6 và 9–10, tổng hợp các yêu cầu duyệt còn thiếu. Phân công theo nhánh thực sự cần, mỗi nhánh một sub-agent riêng:

| Sub-agent | Phạm vi |
|---|---|
| Khảo sát | Bước 1 mục 2–3, chỉ đọc cấu hình và metadata Workspace |
| Thiết kế dữ liệu/Excel | Bước 2 mục 1–4 khi cần bổ sung schema; khác sub-agent khảo sát |
| Security | Bước 2A cho từng Object mới, dùng `$user-permission` |
| Process | Bước 3, dùng `$process-creator`; phần phụ thuộc backend hoàn tất sau bước 9 |
| Backend | Bước 7–8 phần backend: khởi tạo local, code HTTP handler, record trigger, background job, inbound webhook, notification/email, logic theo cơ cấu tổ chức, unit tests/coverage, kiểm thử tích hợp |
| Frontend | Bước 7–8 phần frontend theo dạng đã xác nhận ở 1A: khởi tạo local, code, unit tests/coverage, kiểm thử browser. Custom component/Federation Page: clone template và đọc `.agents/skills` trước khi code |

- Backend và frontend không giao chung một sub-agent. Tái sử dụng sub-agent phụ trách xuyên suốt bước 7–8 và các vòng sửa lỗi; không tạo sub-agent cho nhánh đã bỏ qua.
- Gói giao việc: yêu cầu và tiêu chí nghiệm thu; loại module và dạng frontend đã được người dùng xác nhận ở 1A; đường dẫn skill/reference cần đọc; Workspace origin; metadata đã có; phạm vi đọc/ghi và file được sửa; đầu ra bàn giao; trạng thái phê duyệt và dependency còn chờ. Nhánh triển khai thêm metadata Object/field đã xác minh, project ID/slug/slot, contract route/input/output/error và fixture được sửa. Không đòi kết quả khảo sát hoặc project chưa tạo làm đầu vào của chính bước khảo sát. Chỉ truyền cách truy cập credential an toàn hoặc tên profile.
- Thứ tự: khảo sát → agent chính kiểm tra bằng chứng, chốt phạm vi → gửi đề xuất loại module/dạng frontend (1A), song song thiết kế Excel nếu cần và schema không phụ thuộc lựa chọn (không bắt đầu workbook khi còn thiếu metadata ảnh hưởng schema) → người dùng xác nhận 1A và duyệt workbook → agent chính ghi và đọc lại schema. Có Object mới: tiếp → sub-agent Security hoàn tất 2A → agent chính nghiệm thu → khởi tạo/code. Trong lúc chờ chỉ khảo sát, thiết kế contract, ma trận quyền và kế hoạch test; không dựng source, scaffold, interface code hay test bằng fixture để đi trước.
- Sau 2A (nếu có), chốt contract chung rồi cho các nhánh độc lập chạy song song trong thư mục riêng. Frontend có thể dùng fixture theo contract khi chờ backend nhưng chưa thay test tích hợp thật. Điều phối lịch test ghi khi dùng chung dữ liệu để các agent không reset hoặc sửa fixture của nhau.
- Chỉ giao quyền thao tác Workspace đã được phép; delegation không thay thế duyệt Excel hoặc duyệt Process.
- Nhận lại source, lệnh build/test, coverage riêng từng project, kết quả kiểm thử và dependency còn thiếu; kiểm tra bằng chứng, giao lại lỗi cho sub-agent phụ trách, nghiệm thu tích hợp. Thông báo “xong” không phải bằng chứng PASS.

### 1. Khảo sát và chọn frontend/backend

1. Đọc [$cogover-overview](../cogover-overview/SKILL.md) rồi các reference tính năng của miền nghiệp vụ liên quan. Danh mục App trong overview không phải danh sách App đã cài.
2. Giao sub-agent khảo sát mục 2–3; truyền yêu cầu nghiệp vụ, phạm vi khảo sát và kết quả đọc overview. Sub-agent dùng [$app-menu-manager](../app-menu-manager/SKILL.md) cùng `$cogover-api-auth` liệt kê App thật trên Workspace (`GET /api/v1/apps`, phân trang `page`/`limit` theo contract), đọc detail/menu của App liên quan; ghi ID, slug, standard/custom, trạng thái, phạm vi quyền, chức năng có sẵn. App bị ẩn/thiếu quyền hoặc list chưa đủ trang không chứng minh App chưa cài: phân biệt chưa có, inactive và chưa xác minh được.
3. Cùng sub-agent dùng [$object-info](../object-info/SKILL.md) đọc Objects, fields, options, metadata, related lists liên quan; có automation cũ thì khảo sát Process theo `$process-creator` trước khi đề xuất thêm. Báo cáo: App/Menu và chức năng có sẵn; mapping Object/field/option/quan hệ đã xác minh; Process liên quan; phần thiếu dự kiến; nguồn bằng chứng API/metadata, phạm vi phân trang và điểm chưa xác minh kèm lý do; không chứa credential.
4. Agent chính kiểm tra báo cáo, giao lại phần thiếu hoặc mâu thuẫn trước khi chốt quyết định phụ thuộc vào phần đó. Lập bảng: yêu cầu → khả năng chuẩn và bằng chứng trên Workspace → phần thiếu → nơi xử lý (cấu hình chuẩn/frontend/backend/Process). Chọn loại module theo phần thiếu thực tế: chức năng chuẩn đáp ứng trọn vẹn thì giải thích và dùng skill chuyên trách, không tạo project rỗng cho đủ bước; người dùng yêu cầu rõ trải nghiệm riêng thì vẫn thiết kế phần custom cần thiết. Có frontend: chọn dạng single page app, custom component hoặc Federation Page theo bảng chọn nhanh trong [Các loại Custom Module và dạng frontend](references/get-started-custom-module.md), dựa trên nơi hiển thị, mức tương tác với form, yêu cầu đồng nhất giao diện và stack sẵn có; hỏi phần chưa rõ thay vì suy đoán.
5. Chốt contract nghiệp vụ dự kiến: routes/methods, input/output, caller (thao tác chỉ dành cho Super Admin hoặc role nhất định: tên role, role ID đã xác minh trên Workspace qua `$user-permission` và phản hồi khi bị từ chối), dữ liệu thay đổi và tiêu chí nghiệm thu. Record trigger (bước 3): từng trigger gồm Object, `timing`, `operations`, `fields`, `changedFields`/`when`/`runWhen`, `writableFields`, mã lỗi và message; ghi rõ quy tắc bắt buộc đặt ở before-change và việc chỉ xử lý tiếp đặt ở after-change. Background job (bước 3): từng job gồm `key`, payload dự kiến, `timeoutMs`, `maxAttempts`, `schedule` (cron năm trường, múi giờ IANA) hoặc nơi enqueue (route/trigger/webhook/job khác), quy tắc idempotency key và điều kiện dừng khi tự enqueue. Tích hợp ngoài: tên và loại secret/credential (OPAQUE, BEARER, BASIC, HEADER), host được phép, nơi dùng trong code; giá trị do người dùng/quản trị viên nhập ở bước 6A. Mã hoá hoặc chữ ký với hệ thống ngoài: thao tác và bên giữ từng key (ai giữ private key, AES key dùng chung do ai cấp), thuật toán theo tài liệu bên kia (AES mode, cách truyền IV và tag nối hay tách, `aad`; RSA padding và digest MGF1; thuật toán chữ ký, `der` hay `ieee-p1363`), encoding (`base64`, `base64url`, `hex`), chuỗi byte được ký, tên secret chứa key và mẫu ciphertext/chữ ký của bên kia để kiểm chứng tương thích. Inbound webhook: route dưới `/hooks/`, bên gửi, chế độ KEY hay HMAC (header chữ ký, prefix, timestamp header, dung sai, encoding theo tài liệu bên gửi), định danh sự kiện để chống trùng và job xử lý tiếp. Notification: sự kiện gửi, người nhận (personnel ID lấy từ `org`, field record hoặc danh sách cố định; có loại người gây ra execution không), notification channel (record ID của `notification_channel` đã xác minh trên Workspace hoặc không dùng kênh), tiêu đề, nội dung, link tới record, người gửi hiển thị (`workspace`/`actor`), có gửi bản sao email không và quy tắc `idempotencyKey`. Email: hộp thư gửi (ID hộp thư dùng chung do quản trị viên cung cấp hoặc hộp thư cá nhân của actor) cùng grant cần thêm vào mục `email` của identity policy, người nhận (địa chỉ, personnel, field email của record), record ghi nhận timeline, file field đính kèm, chữ ký và quy tắc `idempotencyKey`. Cơ cấu tổ chức: câu hỏi cần trả lời (quản lý gần nhất hay cả chuỗi, thuộc phòng ban hay cả phòng con), phòng ban dùng làm gốc (phòng ban của record hay phòng ban chính của nhân sự), có bỏ nhân sự chưa có tài khoản (`accountOnly`), cách xử lý chuỗi rỗng, `vacantDepartmentIds`, `truncated`, và phòng ban/nhân sự test đã xác minh qua `$user-permission`. Custom component: Object, layout, slug item Related List/field sẽ thao tác và expose key dự kiến. Federation Page: App/slug, danh sách route và mục menu sẽ gắn.

### 1A. Xác nhận loại module và dạng frontend với người dùng

Bắt buộc trước khi phát triển, kể cả khi yêu cầu ban đầu đã nói "tạo module" hoặc đã nêu sẵn một dạng.

1. Agent chính gửi đề xuất gồm: loại module (frontend, backend hoặc cả hai); với frontend là dạng single page app, custom component hoặc Federation Page; lý do theo bảng chọn nhanh và bằng chứng khảo sát; dạng thay thế gần nhất và lý do không chọn; tác động của lựa chọn (template bắt buộc hay stack tự chọn, nơi hiển thị, cấu hình cần thêm trên layout hoặc menu, cách kiểm thử, giới hạn); câu hỏi còn mở. Người dùng đã chỉ định dạng: vẫn tóm tắt lựa chọn, nêu khác biệt một lần nếu phân tích cho thấy dạng khác phù hợp hơn, rồi theo quyết định của người dùng.
2. Chờ người dùng xác nhận rõ ràng trong hội thoại. Yêu cầu chung ban đầu, im lặng, đồng ý của sub-agent hoặc việc đã duyệt workbook không thay thế xác nhận này. Người dùng chọn phương án khác thì lập lại kế hoạch theo phương án đó trước khi tiếp tục.
3. Trước khi có xác nhận chỉ làm: khảo sát bổ sung, contract, ma trận quyền, kế hoạch test và thiết kế workbook ở bước 2 mục 1–5 khi schema không phụ thuộc lựa chọn; có thể gửi yêu cầu xác nhận 1A cùng yêu cầu duyệt Excel nhưng mỗi mục phải được trả lời riêng. Không tạo Project, không clone template hoặc scaffold, không ghi schema, không tạo policy/key/Process, không viết code.
4. Sau xác nhận: ghi loại module, dạng frontend, lý do và thời điểm xác nhận vào gói giao việc cho sub-agent và báo cáo bàn giao. Đổi dạng giữa chừng, ví dụ custom component sang Federation Page, phải xin xác nhận lại theo cùng cách kể cả khi đã có code.

### 2. Thiết kế dữ liệu và duyệt Excel

Agent chính xác định có cần bổ sung schema. Nếu cần, giao sub-agent thiết kế dữ liệu/Excel (vai chuyên gia thiết kế cơ sở dữ liệu) làm mục 1–4 với báo cáo khảo sát, mapping metadata, yêu cầu dữ liệu/quyền/truy vấn và phạm vi delta dự kiến; agent chính làm mục 5–7. Schema đã đủ thì ghi kết luận, bỏ qua workbook và tạo Object.

1. Xác định thực thể, nguồn dữ liệu chuẩn, quan hệ một-nhiều/nhiều-nhiều, vòng đời, khóa nghiệp vụ, yêu cầu chống trùng, field bắt buộc, kiểu số/ngày/tiền, quyền và truy vấn chính. Tái sử dụng Object nền tảng và Object nghiệp vụ đã có; không tạo bản sao chỉ vì khác tên hiển thị.
2. Phân loại: tái sử dụng nguyên trạng, bổ sung field/option/quan hệ hoặc tạo Object mới; chỉ thêm phần thiếu, ghi rõ lý do và tác động với dữ liệu/cấu hình đang dùng. Mỗi Object mới có ma trận Create/View/Edit/Delete: người dùng nào được thao tác trực tiếp, phạm vi record/field và action nào chỉ backend được thực hiện. Object do backend quản lý là bảng dữ liệu cho logic ứng dụng, có thể chặn toàn bộ hoặc một phần CRUD trực tiếp; không nhầm với Object nền tảng có sẵn.
3. Schema đã đủ: sub-agent báo lại kết luận và lý do, không tạo workbook rỗng. Cần bổ sung: dùng [$create-cogover-objects](../create-cogover-objects/SKILL.md) tạo workbook `.xlsx` theo hướng dẫn spreadsheet và validator của skill đó, sửa mọi lỗi trước khi bàn giao; phân biệt Object mới với delta trên Object hiện có, chỉ rõ lookup tới Object đã tồn tại để không tạo trùng. Bàn giao file `.xlsx` tại đường dẫn agent chính truy cập được, tóm tắt delta/lý do/tác động, ma trận quyền, mapping lookup, kết quả kiểm tra và các điểm còn cần quyết định; không tạo hoặc sửa schema trên Workspace.
4. Mỗi Object có đúng một record-name slug `name`, type Short text hoặc Auto number; tiền dạng số dùng Decimal. Giữ slug/ID tham chiếu thật, không tạo lại field hệ thống.
5. Agent chính mở workbook, đối chiếu với yêu cầu, metadata đã khảo sát và bằng chứng kiểm tra; giao lại sub-agent thiết kế sửa nếu thiếu hoặc sai. **Bắt buộc đưa file Excel đã rà soát cùng tóm tắt delta và ma trận quyền để người dùng duyệt trước khi ghi schema**; yêu cầu chung “tạo module” không thay thế duyệt này. Trong lúc chờ chỉ hoàn thiện thiết kế và kế hoạch test, chưa tạo Object/field hoặc viết code.
6. Sau khi workbook được duyệt: dùng `$object-info` đọc lại state mới nhất, tạo Object/field theo dependency và đúng bản đã duyệt; quan hệ vòng thì tạo Object trước rồi thêm lookup sau khi có ID. Field chỉ backend ghi (bộ đếm, snapshot, trạng thái do backend cập nhật) tạo với `creatable: 0` và `manual_modify_allow: false` theo mục Cờ tạo và sửa thủ công của `$object-info`; backend ghi bằng `data.asSystem()` hoặc trigger before-change gán qua `writableFields` (thiết lập nhập thủ công và quyền sửa field của bên ghi không áp dụng cho field do trigger sửa; field formula, auto number, rollup và field hệ thống vẫn không sửa được), không dùng security rule `exclude` thay cho hai cờ này. Delta thay đổi do Workspace đã đổi thì chỉ xin duyệt lại phần thay đổi thực chất.
7. Đọc lại toàn bộ schema đã ghi, so với workbook; ghi mapping ID/slug/options dùng cho code và Process. Thao tác dở dang: liệt kê phần đã tạo, tra lại trước retry, không tự xóa để làm lại.

### 2A. Tạo security rules cho từng Object mới trước khi code

Bắt buộc khi bước 2 tạo Object mới; giao một sub-agent Security riêng. Chỉ tái sử dụng Object hoặc bổ sung field thì không tự tạo lại bộ rules hay đổi quyền ngoài phạm vi yêu cầu; đánh giá quyền hiện có và xử lý delta được yêu cầu bằng `$user-permission`.

1. Agent chính giao mapping Object/field đã đọc lại, ma trận quyền đã chốt, phạm vi thao tác được phép và cách truy cập credential an toàn. Sub-agent đọc bản hiện tại của `$user-permission` (mục “Object do backend quản lý và rule giữ chỗ”), [Rule giữ chỗ không chọn nhân sự](../user-permission/references/api-object-security-rules.md#rule-giữ-chỗ-không-chọn-nhân-sự) và `$cogover-api-auth`; đọc rules/Role liên quan trước khi ghi để tránh trùng khi retry và phát hiện quyền đã được cấp. Không tự mở rộng Role để làm test thành công.
2. Mỗi Object mới có bộ mặc định **bốn rules riêng: một Create, một View, một Edit, một Delete**, kể cả action không cho người dùng thực hiện trực tiếp; không gộp action, chỉ bổ sung rule cùng action khi audience, điều kiện record hoặc field scope khác nhau. Create dùng `type: 1` với duy nhất scope `create` và vẫn gửi `filter` rỗng theo `$user-permission` (server bắt buộc); ba action còn lại dùng `type: 2`, chỉ cấp scope của action chính, các scope khác `none`/`no`. Action chỉ backend được thực hiện: rule giữ chỗ active (`status: 1`) với đúng `personnelFilters: [{"type": 1, "op": "include", "personnelId": null}]` và scope hợp lệ của action. Audience này không khớp nhân sự nào: rule giữ slot action và duy trì mặc định từ chối nhưng không ghi đè quyền do active rule khác cấp, nên phải rà mọi active rule để chắc không rule nào cấp lại action đang muốn chặn. Không bỏ rule, thay `null` bằng ID giả hoặc tắt rule giữ chỗ để biểu diễn chặn.
3. Sub-agent tạo rules trong phạm vi được phép; đọc lại danh sách không lọc trạng thái và detail của từng rule. Bàn giao mapping Object ID/slug → rule ID/action/type/status/audience/filter/scopes, đối chiếu ma trận quyền, kết quả kiểm thử trực tiếp theo `$user-permission` và phần chưa kiểm thử. Request dở dang: đọc lại trước retry, không tạo bốn rule mới chồng lên bộ đã có.
4. **Agent chính chờ sub-agent hoàn tất và nghiệm thu trước khi chuyển sang bước 7–8:** mọi Object mới đủ bốn slot action active, rule giữ chỗ có `personnelId` là JSON `null`, phạm vi quyền khớp thiết kế, không có grant ngoài ý muốn từ rule khác và có bằng chứng đọc lại. Thiếu rule, cấu hình sai, thao tác lỗi hoặc sub-agent chưa hoàn tất thì chưa được code; không có công cụ sub-agent thì báo rõ dependency và dừng trước bước code. Kiểm tra cấu hình không thay thế kiểm thử runtime: thiếu credential/fixture thì ghi rõ giới hạn và theo dõi ca test còn thiếu. Ca backend dùng quyền hệ thống chạy sau khi backend sẵn sàng ở bước 8–9; không yêu cầu backend tồn tại để hoàn tất bước cấu hình này.

### 3. Chọn record trigger, background job hoặc Process khi cần chạy theo sự kiện, việc nền hoặc lịch

- Cần chạy theo sự kiện create/update/delete của record và quy tắc phải áp cho mọi nguồn ghi (giao diện web, API, import, Process, module khác): từ chối hoặc điều chỉnh giá trị trước khi lưu, hoặc xử lý tiếp ngay sau khi lưu (ghi record liên quan, gọi hệ thống ngoài) thì dùng **record trigger** của Custom Backend Module (`@cogover/sdk` từ `0.6.0`): before-change cho quy tắc bắt buộc, after-change cho việc tiếp theo best-effort. Trigger là một phần của version backend, được áp dụng khi activate và không cần Process; thiết kế ở bước 1 mục 5, code/test ở bước 8, kiểm thử trên Workspace ở bước 9 theo [Record trigger quick start](references/get-started-record-trigger.md). After-change gửi được notification/email cho người liên quan, ví dụ quản lý tìm bằng `org`. Không đủ cho việc dài hơn `timeoutMs`, cần retry hay chạy theo lịch (background job bên dưới) và cho luồng nhiều bước có người tham gia, chờ phê duyệt bằng User Task, gọi AI Agent (Process).
- Cần việc nền của backend: xử lý dài chia trang, gọi hệ thống ngoài có retry lỗi tạm thời, chạy trễ (`delayMs`/`runAt` tối đa 30 ngày) hoặc chạy theo lịch cron (`schedule` với múi giờ IANA, mốc bị lỡ khi Project không active không chạy bù) thì dùng **background job** (`@cogover/sdk` từ `0.8.0`, Workspace đã bật job; `JOBS_DISABLED` thì báo dependency): khai báo `defineJob`, liệt kê trong named export `jobs`, enqueue từ route, trigger after-change, inbound webhook, job khác, CLI hoặc management API. Job là một phần của version backend: lịch có hiệu lực khi activate, activate version khác thay lịch, deactivate gỡ lịch. Run theo lịch, run do quản trị viên/CLI enqueue và run enqueue từ webhook chạy danh tính system (policy `allowInternalSystem: true` ở bước 5); run enqueue từ route/trigger giữ danh tính người gọi/người sửa record. Job gửi được notification/email, ví dụ nhắc hạn hằng ngày. Không dùng job cho việc phải trả kết quả trong cùng HTTP response, cho luồng cần người tham gia (User Task)/AI Agent, hoặc để thay Process mà người dùng muốn tự cấu hình không cần code. Thiết kế ở bước 1 mục 5, code/test ở bước 8, kiểm thử trên Workspace ở bước 9 theo [Background job quick start](references/get-started-background-jobs.md).
- Các trường hợp còn lại cần Process: luồng nhiều bước có người tham gia, chờ phê duyệt, gọi AI Agent, hoặc automation/lịch/thông báo mà người dùng muốn tự cấu hình trên giao diện. Thông báo hoặc email gắn với logic của backend (người nhận tính từ cơ cấu tổ chức hay dữ liệu module, nội dung từ kết quả xử lý) gửi bằng `notifications.send`/`email.send` trong backend; không gửi cùng một thông báo bằng cả backend và node Send Notification/Send Email của Process. Cần Process thì giao sub-agent Process riêng dùng [$process-creator](../process-creator/SKILL.md) xác định event, conditions, timezone/lịch, chống lặp, output cần kiểm chứng và thiết kế Triggered Flow hoặc Scheduled Flow phù hợp. Custom Module xử lý trọn vẹn yêu cầu trong request hiện tại thì bỏ qua cả ba. Không dùng đồng thời record trigger và Triggered Flow cho cùng một quy tắc trên cùng Object, không dùng đồng thời job theo lịch và Scheduled Flow cho cùng một việc; ghi nơi xử lý vào bảng ở bước 1 mục 4.
- Backend đăng ký trigger qua named export `triggers` và lịch qua `schedule` của job trong named export `jobs`; không có cơ chế lịch nào khác. Không dùng timer trình duyệt hoặc tiến trình local thay cho lịch chạy trên Workspace; máy chủ local không chạy job hay cron.
- Sub-agent gửi flow cụ thể để agent chính tổng hợp bước xác nhận của `$process-creator`, tái sử dụng xác nhận đã có cho đúng flow. Đủ phê duyệt thì sub-agent tạo, đọc lại, kích hoạt và kiểm thử theo skill đó, trả Process ID/link, instance và bằng chứng hiệu ứng nghiệp vụ.
- Process phải gọi backend chưa publish: thiết kế ở bước này, hoàn tất cấu hình phụ thuộc và kích hoạt sau bước 9. Không kích hoạt flow có URL/ID giả hoặc dependency chưa sẵn sàng.
- Chỉ nối Process → backend khi contract hỗ trợ cách gọi và danh tính cần thiết: Project key không dùng để gọi production; session export ngắn hạn không phải credential bền vững để nhúng vào lịch Process. Hệ thống ngoài cần gọi backend không có phiên người dùng thì dùng inbound webhook (bước 6A), không nhúng session export. Chưa có cơ chế được hỗ trợ thì báo chính xác dependency còn thiếu và hoàn thành các phần độc lập.

### 4. Tạo Project trên Workspace

1. Đọc API reference đúng nhánh. Qua phiên Workspace với service `4`, list đủ các trang cần thiết để kiểm tra trùng slug, rồi create hoặc tái sử dụng đúng project đã được chỉ định. CLI publish/activate không tạo Project.
2. Backend: `POST /api/v1/ts-projects`. Frontend: `POST /api/v1/ts-projects/frontend`. Body gồm `name`, `slug`, `description` khi cần. Slug bất biến theo `[A-Za-z_][A-Za-z0-9_]{0,99}`, không áp regex slug Process; ID do server cấp.
3. Đọc lại project vừa tạo. Ghi backend `projectId`/`slug`; frontend `projectId`/`slug`/**`slugSlot`**. Full-stack có hai project với cấu hình riêng. Ba dạng frontend dùng cùng loại Project và cùng `slugSlot`: single page app mở `/{slugSlot}/index.html`, custom component dùng `{slugSlot}/Components/<Tên>` trên layout, Federation Page dùng đoạn `c{N}` với `N` lấy từ `_cm_{N}`. Một project theo template có thể chứa đồng thời custom component và Federation Page; single page app là project riêng.

### 5. Cấu hình Project policy nếu cần

- Frontend thuần: bỏ qua, frontend không có identity-policy endpoint trong contract này. Backend dùng quyền caller mặc định, không gọi `data.asSystem()`/`data.asUser()`, không gửi email và không có execution thiếu người dùng cần đọc dữ liệu hay `org`: không tạo policy vô cớ. Notification không cần grant. Các API đặc biệt khác kiểm tra theo SDK contract; identity policy không mở được mọi capability.
- Action bị chặn trực tiếp trên Object do backend quản lý (bước 2A): backend dùng `data.asSystem()` với policy giới hạn đúng caller/Object/operation cần thiết. Code backend dùng quyền caller không tự vượt security rules; trước khi dùng quyền hệ thống phải kiểm tra người gọi có quyền thực hiện nghiệp vụ, policy không thay thế kiểm tra này.
- Dùng quyền ủy quyền: đọc mục Identity policy trong Backend API Reference. Thiết kế schema version `2`, giới hạn caller personnel, Object và operation cần thiết; `data.asUser` giới hạn thêm personnel đích. Resolve ID/slug thật; khi sửa giữ grant ngoài phạm vi.
- Gửi email (`email.send`): thêm mục `email` theo mục [Người gửi email](references/custom-backend-module-api-reference.md#người-gửi-email) của Backend API Reference. `workspaceMailboxIds` chỉ gồm hộp thư dùng chung contract cần (tối đa 50, ID do quản trị viên cung cấp, không đoán), `allowActorMailbox: true` chỉ khi gửi `from: "actor"`; mục phải cấp ít nhất một người gửi và policy có thể chỉ gồm `email`. Grant chỉ áp dụng cho caller trong `callerPersonnelIds`; job theo lịch, inbound webhook và trigger do hệ thống ghi cần thêm `allowInternalSystem: true` và không dùng được `"actor"`. Lưu policy không kiểm tra hộp thư tồn tại: hộp thư sai hoặc bị vô hiệu hoá chỉ lộ ra khi gửi (`NotFoundError` với `resource` là `"mailbox"`), nên sau khi duyệt xác minh bằng `email.senders()` trước khi gửi thật. Khi sửa mục `email` giữ nguyên grant `data.asSystem`/`data.asUser` ngoài phạm vi và ngược lại.
- Policy hợp lệ phải có ít nhất một trong `data.asSystem`, `data.asUser` và `email`; policy chỉ đặt `allowInternalSystem` mà không có grant nào là không hợp lệ. Execution không có người dùng chỉ đọc `org` hoặc chỉ gửi notification vẫn cần policy hợp lệ có `allowInternalSystem: true`: dùng grant module thực sự cần, không thêm grant rộng chỉ để policy hợp lệ; không có grant nào cần thiết thì nêu với người dùng để chọn grant hẹp nhất hoặc chuyển việc sang execution có người dùng.
- Lưu editable policy → đọc lại → activate policy cho nhu cầu phát triển. Editable policy khác snapshot bất biến đã duyệt cho từng version; mỗi lần sửa đưa editable policy về `DRAFT`.
- Sau publish có version `READY`: approve policy cho **đúng version** rồi đọc snapshot, kiểm tra revision/hash/grants theo reference. Activate version không tự duyệt policy; policy đã approve trước đây không mặc nhiên áp dụng cho version mới.
- Record trigger: `data.object()` trong handler chạy dưới danh tính người tạo ra thay đổi với quyền của người đó, nên record người này không thấy sẽ nằm trong `missingIds` của `getMany`; cấp quyền View cho Object tham chiếu hoặc dùng `data.asSystem()` (before-change vẫn chỉ đọc) với policy đúng phạm vi. Thay đổi không có người dùng (`invocation.identity` là `"system"`) chỉ gọi được `data.object()` khi identity policy đã duyệt đặt `allowInternalSystem: true`; nếu không, handler vẫn nhận `records` nhưng lời gọi dữ liệu và `org` ném `PermissionDeniedError` với reason `IDENTITY_NOT_GRANTED`. Xác định nguồn ghi thực tế của Object trước khi quyết định policy.
- Background job và inbound webhook: run theo lịch, run do quản trị viên/CLI/management API enqueue, lời gọi inbound webhook (`invocation.identity` là `"inbound"`, `user` là `null`) và job được enqueue từ lời gọi đó đều thực hiện `data.object()` và đọc `org` dưới danh tính system, nên cần policy đã duyệt với `allowInternalSystem: true` và grant giới hạn vào Object/thao tác thật sự dùng (gửi email thêm grant hộp thư); duyệt lại cho từng version mới trước khi activate, chỉ activate version chưa cấp quyền record. Run enqueue từ route hoặc trigger giữ danh tính người gọi/người sửa record; `data.asUser()`/`data.asSystem()` vẫn theo policy như thường.
- Policy không cấp quyền chạy module nói chung và không thay thế quyền của user đích; permission ceiling của Project key chỉ thu hẹp quyền. Không thêm grant rộng để che lỗi `403`.

### 6. Tạo Project key nếu backend local cần kết nối Workspace

1. Frontend bỏ qua. Backend xử lý thuần túy có thể test bằng fixture không cần key; backend local cần gọi SDK capability thật hoặc chạy record trigger với bản ghi đã lưu thì cần key; thử handler after-change có ghi, `jobs.enqueue`, push, `notifications.send` hoặc `email.send` trên local cần key cho phép writes; chỉ đọc `org` (và `email.senders()`) thì key chỉ đọc là đủ, khi đó chạy máy chủ local với `--allow-writes=false` (bắt buộc với key chỉ đọc). Development Session mặc định không đọc secret, không dùng credential và không dùng key `{ secret }` cho `crypto` (`SECRETS_NOT_ALLOWED`, `FETCH_BLOCKED`) trừ khi quản trị viên cho phép; máy chủ local không chạy job, lịch hay xác thực inbound, nên các phần này kiểm chứng trên version đã publish ở bước 9.
2. Đọc mục Project key; resolve caller personnel, hạn dùng và `permissionCeiling` schema version `1` theo hành vi test. Cho phép writes/asSystem/asUser chỉ khi cần trong phạm vi bài toán.
3. Tạo key bằng `POST /api/v1/ts-projects/{projectId}/keys`, hoặc tái sử dụng key còn hiệu lực đúng project/caller và quyền. Không rotate key đang được người khác dùng chỉ để lấy lại secret.
4. `projectKey` chỉ trả một lần lúc create/rotate: chuyển trực tiếp vào credential store hoặc prompt ẩn của CLI qua kênh an toàn, không in raw response; lưu metadata key ID/caller/hạn dùng để kiểm chứng, không lưu secret vào tài liệu.
5. `cogover-dev login --profile <PROFILE>` rồi `doctor` theo reference CLI. Không dùng Workspace API key thay Project key, hoặc ngược lại.

### 6A. Tạo secret, credential và inbound access nếu backend tích hợp hệ thống ngoài

Chỉ khi contract ở bước 1 mục 5 có tích hợp ngoài; cả hai nhóm là thao tác quản lý dùng Workspace API key/session với service `4` và quyền Workspace SuperAdmin (Project key không đủ; `403` hoặc `SECRETS_DISABLED` thì báo dependency, không tự nới quyền). Cách dùng trong code và giới hạn: [Secrets và credentials quick start](references/get-started-secrets-and-credentials.md), [Inbound webhook quick start](references/get-started-inbound-webhooks.md) và các mục Secret, Inbound access của [Backend API Reference](references/custom-backend-module-api-reference.md).

1. **Secret/credential:** tạo theo đúng tên đã chốt (`[A-Za-z][A-Za-z0-9_]{0,63}`, cùng tên dùng được giữa các môi trường) bằng `cogover-dev secrets set <name> --kind opaque|bearer|basic|header [--header-name <Header>] [--allowed-host <host>]...` (giá trị nhập qua prompt ẩn, `--value-stdin` hoặc `--value-file`; không truyền trên dòng lệnh, không in) hoặc `POST /api/v1/ts-projects/{projectId}/secrets`. Credential (BEARER/BASIC/HEADER) bắt buộc danh sách host được phép không rỗng (host chính xác hoặc `*.suffix`); OPAQUE không có host và là loại duy nhất code đọc được. Secret thuộc Project, dùng ngay cho version active không cần publish; xoay bằng `secrets set` cùng tên (giữ loại/host, tăng `valueVersion`), không xoá rồi tạo lại. Agent chuẩn bị lệnh và xác minh bằng `cogover-dev secrets list` hoặc `secrets/list` (chỉ metadata); người dùng/quản trị viên nhập giá trị thật. Dữ liệu thử nghiệm của quick start (`httpbin.org`, token demo) chỉ dùng trên Workspace test. Key cho `crypto` là secret OPAQUE: AES key 16/24/32 byte lưu dạng văn bản, base64 hoặc hex (code khai báo `encoding` tương ứng); RSA/EC key lưu văn bản PEM nhiều dòng (giá trị secret tối đa 8 KiB) qua `--value-file` rồi xoá file tạm, private key ở dạng PKCS #8 (hoặc PKCS #1, SEC1) chưa mã hoá, không nhận `ENCRYPTED PRIVATE KEY` hay key OpenSSH; RSA 2048–4096 bit, EC P-256/P-384/P-521; public key hoặc certificate của đối tác cũng nên lưu thành secret để xoay không cần publish. Cặp key do phía Cogover giữ được tạo ngoài Cogover bởi người dùng/quản trị viên; chỉ public key được gửi cho bên kia.
2. **Inbound access:** tạo sau khi version có route `/hooks/...` đã activate ở bước 9, một access cho mỗi bên gửi, `--route-prefix` hẹp nhất có thể, `--expires-at <ISO-8601>` cho quyền tạm thời: `cogover-dev inbound create <name> --mode key|hmac --route-prefix /hooks/<route> [--json]` hoặc `POST /api/v1/ts-projects/{projectId}/inbound`. Chế độ KEY: `inboundKey` chỉ trả về một lần khi create/rotate, chuyển cho bên gửi qua kênh an toàn, không in vào chat/log/báo cáo; `rotate` cấp key mới và vô hiệu key cũ ngay. Chế độ HMAC: secret dùng chung qua `--hmac-secret-stdin`, cấu hình `--hmac-header`, `--hmac-prefix`, `--hmac-timestamp-header`, `--hmac-tolerance`, `--hmac-encoding` khớp cách ký của bên gửi (không timestamp thì chỉ ký raw body; có thì ký `<timestamp>.<raw body>`); đổi secret qua `update`, không rotate. Thu hồi bằng `revoke` là vĩnh viễn và không đổi được chế độ. Ghi metadata (inbound ID, name, mode, route prefix, URL gốc kết thúc bằng `/hooks/{inboundId}`, hạn dùng) để kiểm thử ở bước 9 và bàn giao; `inbound list` chỉ trả metadata.

### 7. Khởi tạo local để build/test

- Có Object mới: chỉ giao khởi tạo sau khi agent chính đã nghiệm thu 2A và cung cấp mapping rules/quyền cho sub-agent backend/frontend.
- Backend (sub-agent backend, tiếp tục code/test ở bước 8): theo Backend quick start với starter public được liên kết; `cogover.json` có Workspace origin, Project ID/slug thật và `projectType: "backend"`. `src/main.ts` là entrypoint: default export HTTP handler, named export `triggers` khi có record trigger và named export `jobs` khi có background job (default export là tuỳ chọn với Project chỉ khai báo trigger/job, khi đó mọi URI gọi module trả `404`); `local/` chỉ là local runner (HTTP runner, `trigger-runner.ts` và test chạy bằng `npm test`), không import từ `src/` và không upload. Project có trigger: `npm ls @cogover/sdk` phải từ `0.6.0` và `cogover-dev --version` từ `0.10.0`; Project có job, secret/credential, crypto hoặc inbound webhook: SDK từ `0.8.0` và CLI từ `0.13.1`; dùng `aesEncrypt`/`aesDecrypt`, `rsaEncrypt`/`rsaDecrypt` hoặc `sign`/`verify`: SDK từ `0.9.0`; đọc `membership.isSuperAdmin`/`roles`: SDK từ `0.10.0` và CLI từ `0.14.0` khi chạy local (CLI cũ hơn không truyền hai giá trị này nên script local thấy `isSuperAdmin: false`, `roles: []`); dùng `org`: SDK từ `0.11.0` (bản cũ hơn build báo `org` không tồn tại) và CLI từ `0.14.0`; dùng `notifications`/`email`: SDK từ `0.12.0` và CLI từ `0.15.0`.
- Frontend single page app (sub-agent frontend, tiếp tục code/test ở bước 8): theo Frontend quick start với Vite TypeScript hoặc stack phù hợp yêu cầu; `base: "./"`, build ra `dist/index.html`, hash routing nếu có SPA. `cogover.json` phải có `projectType: "frontend"` (bỏ qua sẽ mặc định backend).
- Frontend custom component hoặc Federation Page (sub-agent frontend, tiếp tục code/test ở bước 8): **trước khi code, clone `https://github.com/cogover/custom-frontend-module-template` vào thư mục riêng của project, chạy `npm ci`, đọc `README.vi.md` và toàn bộ `.agents/skills/*/SKILL.md` trong bản đã clone** (`custom-module-foundation` bắt buộc; `custom-module-form-builder` cùng `src/types/form-builder.d.ts` cho custom component; `custom-module-api` khi gọi API; `custom-module-i18n` khi có đa ngôn ngữ) và báo lại agent chính các quy tắc ảnh hưởng contract. Không tự dựng project React khác, không sao chép rời rạc vài file của template. Giữ nguyên `'./CustomApp': './src/App.tsx'` trong `exposes`, `base: './'`, route tương đối trong `src/routes.tsx`, `Link` nội bộ, asset trong `src/assets`; không hardcode `_cm_N`; không đưa key/secret vào biến `VITE_*`. `.env.local` chỉ chứa `VITE_WORKSPACE_NAME` (và `VITE_ENVIRONMENT` khi suffix domain khác mặc định của `vite.config.ts`), không commit. `cogover.json` đặt tại root template với `projectType: "frontend"`. Template chưa có unit-test runner: bổ sung runner phù hợp (ví dụ Vitest cùng Testing Library) đạt điều kiện coverage của bước 8; `npm run lint` và `npm run build` (typecheck và Vite build) phải pass.
- Mỗi sub-agent cấu hình unit-test runner và coverage phù hợp stack ngay khi khởi tạo: lệnh tái chạy được (ví dụ `npm run test:unit:coverage`), ngưỡng **line coverage >= 70% cho riêng project đó**, exit code khác 0 khi có unit test thất bại hoặc coverage dưới ngưỡng.
- Kiểm tra Node.js >= 20, CLI version/help và dependency thực tế; `auth session` cần CLI >= 0.9.0. Dùng Workspace origin đã xác định, không để CLI rơi về runtime mặc định.
- Full-stack: hai thư mục/cấu hình độc lập; proxy frontend local tới backend loopback theo [Full-stack integration](references/full-stack-integration.md).

### 8. Code và test local

Sub-agent backend và frontend làm phần của mình, viết unit tests cùng lúc code và tự sửa lỗi tới khi đạt điều kiện bên dưới; agent chính phối hợp kiểm thử xuyên hai project.

1. Code theo contract nghiệp vụ và schema đã xác minh. Backend:
   - Dùng public `@cogover/sdk`, chỉ import package được hỗ trợ và local source; không đưa Node built-in, local runner, dynamic import hoặc `require()` vào code publish. Đọc [SDK usage guide](references/cogover-sdk-usage-guide.md) rồi [SDK API reference](references/cogover-sdk-api-reference.md) cho router, data, identity và API tích hợp thực sự sử dụng; TypeScript config theo mục khuyến nghị trong usage guide. API chưa quen: tra bảng README của [cogover-sdk-samples](https://github.com/cogover/cogover-sdk-samples) rồi đọc file sample tương ứng; sample là minh hoạ cách gọi, không thay contract nghiệp vụ.
   - Sinh `workspace.d.ts` từ metadata Workspace theo mục Khai báo Workspace; sinh lại sau mỗi thay đổi schema. SDK Schema API chỉ đọc; tạo/sửa schema vẫn qua `$object-info`.
   - HTTP ngoài: ưu tiên import `fetch` từ SDK để local không âm thầm dùng native fetch. Project state không dùng lưu secret. Cần checkpoint hoặc concurrency thì đọc mục state/locks; lock không tạo transaction hay exactly-once.
   - Dùng caller identity mặc định, kiểm tra input, chỉ đọc field cần thiết, xử lý kết quả thiếu/null, phân trang và lỗi từng phần. Không tin `personnelId` do client gửi để tự nâng quyền. Thông báo API trả client dùng tiếng Anh.
   - Quy tắc theo Super Admin/role (SDK từ `0.10.0`): chỉ đọc `invocation.user.membership.isSuperAdmin` và `roles` từ context, không nhận role hay cờ quản trị do client gửi. So theo role `id` đã xác minh trên Workspace đích (không theo tên), đặt ID thành hằng số hoặc cấu hình có tên rõ ràng; `invocation.user` là `null` với danh tính `system`/`inbound` nên xử lý rõ nhánh này (từ chối hoặc bỏ qua theo contract) thay vì truy cập thẳng. Tách hàm kiểm tra thuần để unit test các ca Super Admin, có role, không role và `user: null`; route từ chối trả `403` kèm message tiếng Anh. Đây là quy tắc nghiệp vụ bổ sung: quyền record/field vẫn do security rules theo danh tính người gọi (Super Admin không tự bỏ qua Object Security Rules); khi cờ/role mở đường cho `data.asSystem()`, kiểm tra trước lệnh ghi đầu tiên và giữ grant của policy giới hạn đúng thao tác. Chạy local: các giá trị được lấy lúc development session bắt đầu, đổi role của user test thì khởi động lại `cogover-dev`.
   - Record trigger: khai báo bằng `defineTrigger` trong module riêng và liệt kê đủ trong named export `triggers`; tách quy tắc nghiệp vụ thành hàm thuần để unit test. `key` ổn định giữa các version; `fields` chỉ gồm field handler đọc; `changedFields`, `when` và `runWhen: "onEnter"` thu hẹp lần chạy và ngăn trigger after-change tự kích hoạt lại; `writableFields` chỉ với before-change và đúng field handler gán. Handler nhận cả lô tới 200 record: gom ID, đọc một lần bằng `records.getMany`, ghi một lần bằng batch, không đọc/ghi trong vòng lặp. Before-change chỉ đọc (ghi record, `fetch`, lock, state, push, notification, email, `jobs.enqueue`, `secrets.get` và mọi thao tác `crypto` bằng key `{ secret }` bị từ chối với reason `TRIGGER_READ_ONLY`; đọc `org`, hash, random và thao tác `crypto` với key truyền trực tiếp, ví dụ `verify` bằng public key, vẫn dùng được), fail-closed và phải xong trong `timeoutMs` (mặc định 2 giây, tổng 8 giây cho mọi trigger before-change của một thao tác ghi): đặt quy tắc bắt buộc ở đây, từ chối bằng `record.addError(field hoặc null, CODE, message)` với code viết hoa, message tiếng Anh. After-change best-effort, có thể chạy lặp hoặc không chạy: idempotent theo `trigger.changeId` cùng `record.id`, ném `RetryableError` chỉ cho lỗi tạm thời, không sửa `record.new`, sửa record bằng `records.update`/batch (thao tác ghi mới, chạy lại trigger trong ngân sách 10 bước) rồi `push.refreshRecords` cho các record đã sửa để người đang mở, kể cả người vừa lưu, tải lại; việc dài hơn `timeoutMs` hoặc cần state/locks thì `jobs.enqueue` với idempotency key theo `changeId`.
   - Push message (`push.refreshRecords`, `push.toast`, `push.message`; SDK từ `0.7.0`): có trong script, route, trigger after-change và Development Session ghi được; cùng cơ chế với action Push Message của Workflow nên trang record chuẩn tự xử lý `refreshRecords` và `toast`. Người nhận mặc định `"viewers"` (người đang mở record đích, bắt buộc `object` và `recordIds`), hoặc danh sách personnel ID đích danh; `exclude: ["actor"]` bỏ người gây ra execution. Gửi là best-effort, không thu hồi được và có thể gửi lặp khi execution chạy lại; mỗi lời gọi tốn một capability call; tối đa 200 record ID và 200 personnel ID; ID/slug không tồn tại ném `NotFoundError`, deployment tắt push ném `PUSH_DISABLED`. Không đưa secret hoặc dữ liệu người dùng không được xem vào toast/message; `links` của toast phải là URL tuyệt đối `http(s)`. `push.message` cho custom component chỉ dùng khi template hoặc `@cogover/client-sdk` đã clone có API lắng nghe được xác minh trong `.agents/skills`; không suy đoán API phía client.
   - Cơ cấu tổ chức (`org`; SDK từ `0.11.0`): chỉ đọc, có trong script, route, trigger (cả before-change), job và Development Session. Lấy người gọi bằng `org.me()` hoặc `invocation.user.membership.personnelId`, không nhận `personnelId` người duyệt hay cấp quản lý do client gửi; ID nhận từ request thì kiểm tra định dạng (ví dụ `^[A-Za-z0-9_-]{1,64}$`) trước khi gọi. Dùng `isManagerOf(managerId, personnelId, { departmentId })` cho kiểm tra quyền duyệt và truyền phòng ban của record khi có (không có `departmentId` thì mọi phòng ban của người tạo đều được xét, nên người quản lý họ ở phòng ban khác cũng nhận `true`); `directOnly: true` khi chỉ quản lý trực tiếp được duyệt. Chọn người duyệt bằng `personnel.managerChain(id, { departmentId, accountOnly: true })`: `tiers[0]` là quản lý gần nhất, nhiều người cùng cấp chung một tier, mỗi người chỉ xuất hiện ở tier gần nhất; xử lý rõ chuỗi rỗng (người đứng đầu, nhân sự không thuộc phòng ban nào hoặc không tồn tại), `vacantDepartmentIds` và `truncated` theo contract thay vì mặc định cho qua. `level` của membership là cấp quản lý (`0` nhân viên, `1` cao nhất), `level` của phòng ban là độ sâu cây (`1` là gốc). Chỉ thêm `withDisplay: true` khi cần tên, email, avatar (thêm tối đa một lần đọc cho mỗi loại dữ liệu và mỗi 200 ID; giá trị hiển thị có thể là `null`); tên theo ngôn ngữ người gọi trừ khi truyền `language`. Đọc nhiều người bằng `getMany` hoặc một trang `members` (mặc định 500, tối đa 2.000, tối đa 200 khi có `withDisplay`; phân trang bằng `nextCursor`), không gọi `get` trong vòng lặp; mọi lời gọi trong một execution đọc cùng một phiên bản cơ cấu. Field nhân sự khác (số điện thoại, custom field) đọc qua `data.object("personnel")` với quyền record. Lỗi: `get` trả `null`, `personnel.managerChain` trả mảng rỗng, `isManagerOf`/`isInDepartment` trả `false` với ID không tồn tại; `members`, `managers`, `managerChain` của phòng ban, `ancestors`, `tree({ rootId })` ném `NotFoundError` (chuyển thành `404`); `ORGANIZATION_UNAVAILABLE` là lỗi tạm thời (HTTP `503` nếu không bắt, ném `RetryableError` trong job/after-change khi cần thử lại). Tách logic chọn người duyệt thành hàm thuần nhận kết quả `org` để unit test các ca nhân viên, phó phòng, trưởng phòng, nhiều phòng ban, chuỗi rỗng và phòng trống.
   - Notification (`notifications.send`; SDK từ `0.12.0`): có trong script, route, trigger after-change, job, lời gọi inbound webhook và Development Session ghi được. `to` là 1–200 personnel ID (chia lô khi nhiều hơn), `exclude: ["actor"]` khi không báo cho chính người gây ra execution; kiểm tra `notified` và `skippedPersonnelIds` (personnel chưa có tài khoản) trong kết quả thay vì coi mọi người nhận đều đã được báo. `channel` là record ID `notification_channel` đã xác minh, đặt thành hằng số hoặc cấu hình có tên rõ ràng; không truyền `channel` thì mọi hình thức gửi đều bật; `email: false` khi không muốn bản sao email. `title` ≤ 200 ký tự, `content` ≤ 16.384 ký tự; `contentType: "html"` chỉ giữ định dạng cơ bản, escape dữ liệu người dùng trước khi ghép HTML; `subtitle` cần `display: "FULL"` (mặc định khi có subtitle); `link.url` là path web app bắt đầu bằng `/` hoặc URL `http(s)` tuyệt đối; `sender: "actor"` ném `ValidationError` khi execution không có người dùng. Không đưa secret hoặc dữ liệu người nhận không được xem vào nội dung.
   - Email (`email.send`, `email.senders`; SDK từ `0.12.0`): có trong script, route, trigger after-change, job, lời gọi inbound webhook và Development Session. Chọn hộp thư theo địa chỉ từ `email.senders()` hoặc ID cấu hình có tên rõ ràng, không nhận mailbox ID từ client; `from: "actor"` chỉ khi có người dùng và policy cho phép. Người nhận là địa chỉ, `{ email, name }` hoặc `{ personnelId }` (tổng tối đa 50 trong `to`/`cc`/`bcc`, `to` bắt buộc trừ khi có `recordEmailFields`); cung cấp đúng một trong `html` và `text` (tối đa 192 KB), `subject` một dòng ≤ 255 ký tự; escape dữ liệu người dùng trong HTML. Cần email trên timeline record (theo dõi lượt mở/bấm, gom thư trả lời) thì truyền `record`; `recordEmailFields` (≤ 20 field email), `attachments` (file field của record, ≤ 10 file, tổng 20 MB) và `appendSignature` (chỉ với `"actor"`) cần `record`, và record phải đọc được bằng danh tính mặc định. Không có `record` thì `delivery: "direct"`. Lời gọi xong khi Cogover tiếp nhận: lưu trạng thái "đã tiếp nhận để gửi" cùng `requestId`, không coi là đã tới hộp thư. Lỗi riêng: `EMAIL_SENDER_NOT_GRANTED` (grant ở bước 5), `NotFoundError` `mailbox`/`personnel`/`record`, `ValidationError`.
   - Chung cho notification và email: mỗi lời gọi là một capability call, không thu hồi được; việc gửi lặp khi execution chạy lại chỉ tránh được bằng `idempotencyKey` (chữ, số, `.`, `_`, `:`, `-`, tối đa 128 ký tự, giữ 7 ngày, key của notification và email tách biệt): trigger after-change dùng `${trigger.changeId}:${record.id}`, job dùng `job.id` kèm ID nghiệp vụ, route dùng khoá nghiệp vụ để bấm lặp không gửi hai lần; `duplicate: true` là kết quả hợp lệ. `DUPLICATE_IN_PROGRESS` nghĩa là lời gọi cùng key đang gửi: thử lại sau giây lát hoặc ném `RetryableError` trong job. Hạn mức mặc định mỗi project 5.000 người nhận notification và 500 email mỗi giờ (`RateLimitError`, không gửi gì): chia đợt lớn qua nhiều run job. `NOTIFICATIONS_DISABLED`/`EMAIL_DISABLED` là dependency của deployment, báo lại thay vì sửa code. Gửi sau khi các thao tác ghi record liên quan đã thành công, không gửi trong vòng lặp đọc/ghi từng record khi gom được một lần gửi cho nhiều người nhận. Tách hàm dựng message (người nhận, tiêu đề, nội dung, key) thành hàm thuần để unit test.
   - Background job (`defineJob`, named export `jobs`, `jobs.enqueue`; SDK từ `0.8.0`): khai báo trong module riêng, liệt kê đủ trong `jobs` (tối đa 50), `key` ổn định giữa các version (key mới là job khác). Tự kiểm tra `payload` như input request (JSON tối đa 64 KiB; truyền ID record thay vì dữ liệu lớn). Handler idempotent và chạy lại an toàn (ít nhất một lần, có thể chạy đồng thời với run khác): kiểm tra trạng thái/record trước khi ghi, dùng `job.id` làm idempotency key khi gọi dịch vụ ngoài, ném `RetryableError` chỉ cho lỗi tạm thời (lỗi khác kết thúc `FAILED` không retry), giá trị trả về bị bỏ qua nên lưu kết quả vào record hoặc state. Mỗi lần thử trong `timeoutMs` (tối đa 60 giây): việc dài xử lý một trang (tối đa 200 record, sắp xếp ổn định, ghi bằng batch) rồi `jobs.enqueue` chính nó với cursor và idempotency key `${job.id}:next`, có điều kiện dừng. Enqueue từ route/trigger/webhook với `idempotencyKey` theo khoá nghiệp vụ (tối đa 128 ký tự, key cũ trả run cũ với `duplicate: true` kể cả đã kết thúc), `delayMs` hoặc `runAt` khi cần chạy trễ; enqueue sau các thao tác ghi record mà job phụ thuộc và không cùng transaction; tối đa 50 lần enqueue mỗi invocation; run đã enqueue không hủy riêng được nên handler đọc lại dữ liệu và bỏ qua việc không còn cần. Lịch: cron năm trường (không tên `MON`, không macro `@daily`), múi giờ IANA mặc định `UTC`, `defineJob` validate cron khi module được nạp (typecheck không phát hiện), mốc bị lỡ không chạy bù, run lịch có thể chồng nhau nên bọc `locks.withLock` (`waitMs: 0`) cho đoạn không được chạy song song.
   - Secret/credential (`fetch({ credential })`, `secrets.get`, key `{ secret }` của `crypto`; SDK từ `0.8.0`): ưu tiên credential khi token nằm trong HTTP header (không tự đặt `authorization` hoặc header trùng với header đã cấu hình); chỉ dùng `secrets.get`/`{ secret }` với secret OPAQUE khi cần chính giá trị, đọc một lần trước vòng lặp (tối đa 20 thao tác secret mỗi invocation, giá trị cache trong invocation); không ghi giá trị vào log, record, state, response hay thông báo lỗi; chỉ trả field cần thiết từ response của dịch vụ ngoài vì response có thể chứa token. `401`/`500` từ dịch vụ ngoài là response `fetch` bình thường: kiểm tra `ok`/`status`; timeout không chứng minh thao tác ghi đã thất bại.
   - Crypto hash/HMAC/random (`sha256`, `hmacSha256`, `randomBytes`, `randomUUID`, `timingSafeEqual`; SDK từ `0.8.0`): lấy từ context hoặc `import { crypto } from "@cogover/sdk"`, mọi thao tác trừ `timingSafeEqual` cần `await` và tính một capability call; so sánh chữ ký/token luôn bằng `timingSafeEqual`; tạo body gửi đi một lần rồi ký đúng chuỗi đó, kiểm tra chữ ký inbound trên `request.rawBody` (không `JSON.stringify(request.body)`), chuẩn hoá encoding/prefix của bên gửi trước khi so; tối đa 256 KiB mỗi lần gọi; chỉ SHA-256 hash/HMAC, không băm mật khẩu; token truy cập chỉ lưu hash kèm hạn; sandbox không có `Buffer` hay `node:crypto`.
   - Mã hoá và chữ ký (`aesEncrypt`, `aesDecrypt`, `rsaEncrypt`, `rsaDecrypt`, `sign`, `verify`; SDK từ `0.9.0`): key qua `{ secret: name }` (AES key thêm `encoding: "base64"` hoặc `"hex"` khi secret lưu dạng đó), không đưa private key/AES key thật vào source, fixture hay log (test chỉ dùng key sinh riêng cho test). AES: mặc định GCM và để Cogover sinh IV ngẫu nhiên, chỉ truyền `iv` khi bên kia quy định và không bao giờ dùng lại IV với cùng key GCM; gửi `iv` cùng ciphertext; tag mặc định nối cuối ciphertext (Java, .NET, Web Crypto), `separateTag: true` khi bên kia cần tag riêng (Node.js, OpenSSL); `aad` phải khớp khi giải mã; CBC chỉ khi bên kia bắt buộc và xác thực thêm bằng HMAC. RSA: mặc định OAEP-SHA256 với MGF1 SHA-256, xác nhận digest MGF1 của bên kia (code Java `OAEPWithSHA-256AndMGF1Padding` thường để MGF1 SHA-1, không khớp cả `OAEP-SHA256` lẫn `OAEP-SHA1`), `PKCS1` chỉ khi bắt buộc; dữ liệu vượt giới hạn RSA thì mã hoá bằng AES với key `randomBytes(32)` rồi chỉ mã hoá key đó bằng RSA. Chữ ký: chọn `algorithm`/`encoding`/`signatureFormat` theo bảng JWT `alg` của SDK API reference (`ES*` cần `base64url` và `ieee-p1363`), ký và xác minh đúng chuỗi byte (`request.rawBody`, `header.payload` của JWT), không dùng `RSA-SHA1` cho chữ ký mới; `verify` trả `false` với chữ ký sai hoặc sai định dạng nên kiểm tra giá trị trả về, không dựa vào exception; JWT có chữ ký hợp lệ vẫn phải kiểm `exp`, `iss`, `aud` trước khi tin. Giải mã thất bại vì bất kỳ lý do gì ném `CogoverApiError` `code: "DECRYPTION_FAILED"`: bắt riêng để trả lỗi nghiệp vụ tiếng Anh, không log ciphertext/plaintext nhạy cảm; plaintext không phải UTF-8 thì dùng `output: "bytes"` hoặc encoding nhị phân. Key/thuật toán/tuỳ chọn sai ném `ValidationError`; `data`, `aad` và ciphertext tối đa 256 KiB, văn bản key tối đa 16.384 ký tự; mỗi lời gọi là một capability call, key `{ secret }` tính vào giới hạn 20 thao tác secret mỗi invocation.
   - Inbound webhook (SDK từ `0.8.0`): đăng ký route dưới `/hooks/` (bên gửi gọi `/hooks/{inboundId}/...`, handler thấy `request.path` không có inbound ID), kiểm tra `invocation.identity === "inbound"` và `invocation.inbound.mode` khi bắt buộc HMAC (lời gọi qua URL thường hoặc local có danh tính người dùng thì trả `403`), validate `request.body` (JSON object; body dạng khác cho `{}` và tự parse từ `rawBody`/`contentType`). Handler ngắn: kiểm tra, ghi dấu đã nhận bằng state `expectedVersion: 0` (xử lý `StateConflictError` khi trùng, thêm định danh bên gửi vào khoá nếu nhiều bên có thể dùng cùng event ID) hoặc `jobs.enqueue` với event ID làm `idempotencyKey`, rồi trả `200`/`202`; nhà cung cấp gửi lại khi không nhận 2xx nên phải chịu được sự kiện lặp, và chặn chữ ký đã dùng của Cogover không bảo đảm tuyệt đối. `request.headers` không chứa Authorization, cookie, inbound key hay header chữ ký đã cấu hình. Cần thêm chữ ký riêng của nhà cung cấp thì kiểm tra bằng `crypto` trên `rawBody`; cách ký của họ khớp chế độ HMAC có sẵn thì cấu hình ở inbound access (bước 6A) để Cogover kiểm tra trước handler.
2. Test logic, input không hợp lệ, thiếu record, quyền được phép/bị từ chối và ghi lặp khi liên quan. Decimal, preview/apply hoặc ghi hàng loạt: đọc [Batch writes và checkpoint](references/batch-writes-and-checkpoints.md), kiểm tra làm tròn, request đồng thời, preview cũ và lỗi từng phần. Chạy typecheck/build và các test phù hợp dự án.
3. Backend có dữ liệu thật chạy qua `COGOVER_LOCAL_PORT=<PORT> cogover-dev run --profile <PROFILE> --allow-writes=false -- npm run dev` cho test đọc; test ghi trong phạm vi yêu cầu dùng `--allow-writes=true` với fixture có marker/ID theo dõi. CLI mặc định cho phép writes; chạy local không đồng nghĩa mock hay dry-run. Record trigger chạy trên cùng server local theo [Record trigger quick start](references/get-started-record-trigger.md): `GET /__cogover/triggers` liệt kê manifest đã chuẩn hoá; `POST /__cogover/triggers/<key>` với `operation` và `changes` (create) hoặc `recordId` kèm `changes` (update/delete), hoặc `records: [...]` cho một lô; đối chiếu `input.records`, `results` (`errors`, `changes`) và `warnings`. Before-change chạy với `--allow-writes=false`; after-change có ghi hoặc push cần server không có cờ này và ghi/gửi thật qua Development Session (Development Session chỉ đọc từ chối push). Push trên local gửi tới người dùng thật của Workspace: khi thử, giới hạn `recipients` là personnel test hoặc record fixture chỉ người test đang mở. Notification và email trên local cũng tới người thật, kể cả địa chỉ ngoài Workspace: chỉ gửi tới personnel test và địa chỉ test đã được người dùng xác nhận, từ hộp thư test hoặc hộp thư được phép thử, gắn `idempotencyKey` và ghi lại `requestId`; session `--allow-writes=false` từ chối `notifications.send`/`email.send` nhưng vẫn gọi được `email.senders()` để xác minh hộp thư khả dụng. Đọc `org` chạy được với session chỉ đọc và dùng cơ cấu thật của Workspace dưới danh tính nhân sự được cấp Project key (khác người dùng của Workspace API key khi gọi production): đối chiếu kết quả `/me`, thành viên phòng ban và chuỗi quản lý với dữ liệu Department/Personnel/Position đọc qua `$user-permission`, thay đổi cơ cấu hiện ra sau vài giây. Runner không đánh giá `when`/`changedFields`/`runWhen` và bỏ field ngoài `fields`; route `/__cogover/` chỉ có trên local. Cogover chỉ gửi sự kiện thật tới version đã activate, nên kiểm thử bằng thay đổi bản ghi thật ở bước 9. Máy chủ local không chạy job, lịch cron hay xác thực inbound: `jobs.enqueue` từ route local tạo run thật trên Cogover chạy code job của version đang active (publish/activate handler job trước khi thử theo cách này; session `--allow-writes=false` từ chối enqueue); route `/hooks/...` gọi local mang danh tính người dùng nên điều kiện inbound trả `403` theo thiết kế; secret, credential và `{ secret }` chỉ dùng được local khi quản trị viên cho phép Development Session, còn hash, random và `crypto` với key truyền trực tiếp chạy được local (chỉ dùng cặp key test sinh riêng, không dùng key thật). Mã hoá/chữ ký: kiểm tra round-trip (`aesEncrypt` → `aesDecrypt`, `rsaEncrypt` → `rsaDecrypt`, `sign` → `verify`), ca ciphertext bị sửa trả `DECRYPTION_FAILED`, chữ ký bị sửa trả `false`, và giải mã/xác minh mẫu do bên kia cung cấp để chứng minh tương thích. Tách logic job/webhook thành hàm thuần để unit test bằng fixture; kiểm chứng retry, lịch, danh tính và chữ ký trên Cogover ở bước 9.
4. Frontend test trên browser: loading, empty, error, thao tác chính, khả năng dùng bàn phím và kích thước màn hình liên quan. Full-stack phải test xuyên frontend → backend → dữ liệu. Custom component: chạy `npm run build-watch` và `npm run preview`, dán URL `http://localhost:5101/#./Components/<Tên>` do preview in ra vào item Federation component của layout test, kiểm tra trên form thật các ca thiếu `formBuilder`, cập nhật field/ô Related List, lỗi giữa chừng và không tự chạy lặp; slug item lấy từ layout qua `$object-layout`, không dùng slug Object. Federation Page: `npm run dev` với `.env.local` trỏ Workspace test, người dùng tự đăng nhập Cogover trên trình duyệt (agent không nhập credential), kiểm tra từng route, `Link` nội bộ và API qua HTTP client của template. URL localhost trên layout chỉ dùng khi debug và phải thay bằng đường dẫn đã deploy ở bước 9.
5. Ghi có thể đã thành công trước khi lỗi: đọc lại state trước retry; nhiều lệnh ghi không mặc nhiên là transaction. Lưu kết quả test và fixture đã chạm tới, không lưu credential.

Điều kiện bắt buộc cho từng project trước khi kết thúc bước 8:

- Toàn bộ unit tests PASS, số thất bại bằng 0, có test thực sự được chạy; không dùng suite rỗng hoặc skip/todo thay các ca cần kiểm chứng. Typecheck/build và các test tích hợp/browser thuộc phạm vi cũng phải thành công.
- **Line coverage >= 70% riêng backend và >= 70% riêng frontend.** Không lấy trung bình hoặc gộp coverage hai project; không dùng branch/function/statement coverage thay line coverage; không cộng E2E/integration coverage vào kết quả unit tests.
- Đo coverage từ unit-test suite trên toàn bộ source ứng dụng của project, kể cả file chưa được test import. Chỉ loại trừ dependency, test/fixture, build output, code sinh tự động, declaration và cấu hình/local runner không thuộc code ứng dụng; ghi rõ cấu hình include/exclude. Không loại code nghiệp vụ, entrypoint hay UI cần chạy để đạt ngưỡng. Backend test logic/validation/error/side effect bằng dependency được kiểm soát; frontend test logic và component/hành vi UI phù hợp stack.
- Lưu cho mỗi project: lệnh test, số test pass/fail, số dòng covered/total, phần trăm lines và đường dẫn báo cáo coverage, khớp source cuối sẽ publish. Sau khi sửa code, chạy lại unit tests với coverage của project bị ảnh hưởng.
- Agent chính kiểm tra báo cáo và ngưỡng, yêu cầu sub-agent sửa nếu chưa đạt. Coverage chưa đo hoặc thiếu báo cáo là chưa đạt, không tự coi là 70%; không chuyển project chưa đạt sang publish.

### 9. Publish, activate và test trên Workspace thật

1. Ghi nhận active version hiện tại của từng project và các thay đổi schema liên quan. Kiểm tra archive không có secret, `.env`, session file, source map hoặc dependency/local runner ngoài phạm vi.
2. `cogover-dev publish` tại đúng thư mục project: backend ZIP chứa `src/main.ts`; frontend phải build trước, ZIP chứa `dist/index.html` (giữ thư mục `dist/`), CLI không build frontend. Publish qua API trực tiếp: upload ZIP private, dùng file metadata thật cùng `idempotencyKey` theo reference; không đoán upload endpoint.
3. Chờ version `READY` hoặc `FAILED` có thời hạn; HTTP `202` chưa phải hoàn tất. Timeout: đọc version đã tạo trước khi publish lại. `FAILED`: sửa nguyên nhân rồi tạo version mới. Ghi version ID và build error an toàn nếu có. Backend có trigger: build evaluate export `triggers` thành trigger manifest và validate Object/field slug tồn tại, `writableFields` không có field chỉ đọc, key không trùng; `TRIGGER_MANIFEST_INVALID` nêu key/slug sai, sửa code rồi publish lại. Backend có job: build evaluate export `jobs` thành job manifest (key, `timeoutMs`, `maxAttempts`, `schedule` đã chuẩn hoá); `defineJob` ném `ValidationError` với cron/múi giờ/key sai khi module được nạp nên chạy máy chủ local hoặc `npm test` trước publish, không chỉ typecheck.
4. Backend có policy: approve/read snapshot đúng version theo bước 5. Preview service `6` phải chỉ rõ version ID và `READ_ONLY`/`READ_WRITE`; preview mặc định `READ_WRITE`, ghi thật và không rollback theo nhóm.
5. Activate đúng version bằng CLI, đọc lại project xác nhận `ACTIVE` và `activeVersionId`. Full-stack: backend sẵn sàng trước frontend phụ thuộc nó; hoàn tất Process còn chờ dependency. Version có trigger: activate áp dụng manifest ngay cho mọi thao tác create/update/delete trên Object từ mọi nguồn; activate version khác thay bằng manifest của version đó, deactivate gỡ toàn bộ. Báo người dùng phạm vi ảnh hưởng trước lần activate đầu tiên trên Object đang có dữ liệu thật; gỡ một trigger bằng cách bỏ khỏi export `triggers` rồi publish/activate version mới. Version có job: activate làm `jobs.enqueue` nhận các key của version đó và bật lịch ngay (xem bằng `cogover-dev jobs schedules`); activate version khác thay lịch, deactivate gỡ lịch; run chạy trễ/retry dùng code của version active tại lúc chạy nên giữ key và định dạng payload tương thích. Báo người dùng trước khi activate lịch ghi dữ liệu thật; dừng một lịch bằng cách bỏ `schedule` rồi publish/activate version mới.
6. Backend: gọi production service `3` bằng session do CLI tạo theo [mẫu cURL](references/cli-session-and-delivery.md); kiểm tra HTTP/body và đọc lại dữ liệu nghiệp vụ. Preview hoặc `READY` không phải bằng chứng production PASS. Route dùng query string: test filter có kết quả/không có kết quả và cursor trên production; local khác Workspace thì áp dụng mục tương thích trong [Full-stack integration](references/full-stack-integration.md). Record trigger: kiểm thử bằng thay đổi bản ghi thật qua `$object-record` hoặc giao diện theo mục 8 của Record trigger quick start, đủ các ca của từng trigger: bị từ chối trả HTTP `400`, `r: 70`, `msg: BEFORE_CHANGE_TRIGGER_REJECTED`, `data.trigger.key`, `meta` theo field slug (`$record` cho lỗi cả bản ghi) và `messages`; điều chỉnh giá trị thì đọc lại record thấy field đã điền; after-change đọc lại sau vài giây và kiểm tra `runWhen: "onEnter"` không chạy lại khi record đã khớp `when`. HTTP `503` với `BEFORE_CHANGE_TRIGGER_FAILED` là handler ném lỗi, hết giờ hoặc ghi trong before-change: mọi thao tác ghi liên quan trên Object bị chặn cho tới khi activate version đã sửa hoặc version cũ hợp lệ. Dọn fixture theo đúng quy tắc trigger (ví dụ đổi trạng thái trước khi xoá). Push message: xác minh trên trình duyệt đã đăng nhập đang mở record đích (record tải lại sau `refreshRecords`, toast hiển thị đúng nội dung/vị trí/thời lượng, người trong `exclude` không nhận) và log Project không có `PUSH_DISABLED`; push best-effort nên không nhận được một lần chưa chứng minh lỗi code, lặp lại với record khác trước khi kết luận.
7. Background job, secret/credential, crypto, inbound webhook, notification, email và cơ cấu tổ chức trên Workspace thật:
   - Background job: `cogover-dev jobs schedules` xác nhận lịch sau activate; enqueue thử bằng `cogover-dev jobs enqueue <key> --payload-file payload.json --idempotency-key <key>` (có `--delay-ms` khi cần) hoặc qua route/trigger, rồi theo dõi `cogover-dev jobs runs --job <key>` và `cogover-dev jobs run {RUN_ID}` tới `SUCCEEDED` với số lần thử mong đợi; đọc lại record/state để chứng minh hiệu ứng nghiệp vụ. Kiểm thử ca lỗi tạm thời (`PENDING` với lỗi `RETRYABLE` rồi `SUCCEEDED` ở lần thử sau, `FAILED` khi hết `maxAttempts`), enqueue lặp cùng idempotency key trả `duplicate: true`, chạy trễ, job tự enqueue trang kế dừng đúng lúc. Run theo lịch chờ mốc kế tiếp; cần thử nhanh thì dùng cron ngắn trên Workspace test rồi publish lại lịch thật. CLI/management API không trả payload, chỉ `payloadBytes`; lịch sử run giữ bảy ngày. `JOBS_DISABLED`, `JOB_NOT_DEFINED`, `IDENTITY_NOT_GRANTED`, `JOB_ACTOR_UNAVAILABLE`, `JOB_TIMEOUT`, `RateLimitError` xử lý theo bảng sự cố của [Background job quick start](references/get-started-background-jobs.md).
   - Secret/credential: `cogover-dev secrets list` có mục `ACTIVE` đúng tên, loại, host trước khi gọi route; gọi route production và xác minh response không chứa, log không ghi giá trị; trong phạm vi được phép, thử host ngoài danh sách nhận `FETCH_BLOCKED` rồi khôi phục; xoay giá trị bằng `secrets set` cùng tên và gọi lại không cần publish. Xoá mục thử nghiệm bằng `secrets delete` (có xác nhận), giữ mục thật.
   - Crypto với key `{ secret }`: gọi route production với dữ liệu thử, kiểm tra round-trip và ca lỗi như bước 8 trên version đã activate; trao đổi mẫu với bên kia (họ giải mã/xác minh được dữ liệu Cogover gửi, Cogover giải mã/xác minh được mẫu của họ) trước khi dùng dữ liệu thật; response và log không chứa key hay plaintext nhạy cảm. `SECRETS_NOT_ALLOWED`, `TRIGGER_READ_ONLY`, `ValidationError` về key xử lý theo [Crypto quick start](references/get-started-crypto.md) và mục Key của SDK API reference.
   - Inbound webhook: tạo inbound access theo bước 6A sau activate; gửi request thử tới `{URL gốc}/{route}` với key (`Authorization: Bearer` hoặc `X-Cogover-Inbound-Key`) hoặc chữ ký HMAC đúng cách ký đã cấu hình (có timestamp header thì ký `<timestamp>.<raw body>`, gửi `--data-binary` đúng chuỗi đã ký) mong đợi `200`/`202` và hiệu ứng (state/job/record); ca sai key, sai chữ ký, body đổi sau khi ký, gửi lại cùng chữ ký/timestamp, route ngoài `routePrefix`, access hết hạn mong đợi `401` trước handler; gửi lại cùng event ID để chứng minh chống trùng; `429` khi vượt 600 request/phút. Bên gửi không cần Workspace session hay header `x-req-*`; response không có envelope. Thu hồi access thử và `unset` biến shell chứa key/secret sau khi xong; access thật giữ lại và bàn giao metadata.
   - Notification: kích hoạt qua route, trigger hoặc job trên version active với người nhận là personnel test đã xác nhận (notification và email không thu hồi được); kiểm tra kết quả `notified`/`skippedPersonnelIds`, rồi trên trình duyệt đăng nhập đúng người nhận xác minh notification trong danh sách thông báo (tiêu đề, subtitle, nội dung, người gửi, link mở đúng trang), hình thức gửi theo kênh và cài đặt riêng của người nhận, người trong `exclude` không nhận; gọi lại cùng `idempotencyKey` trả `duplicate: true` và không tạo notification mới. `NotFoundError` với `resource` `notificationChannel` là sai channel ID, với `resourceId` `notification_channel` là Workspace chưa thiết lập notification.
   - Email: approve policy có mục `email` cho đúng version trước khi thử; xác nhận `email.senders()` trả đúng hộp thư mong đợi; gửi tới địa chỉ test, người dùng xác nhận thư tới nơi (gửi bất đồng bộ nên chờ và kiểm tra lại trước khi kết luận lỗi) và, khi có `record`, hoạt động email xuất hiện trên timeline record (`delivery: "activity"`) với file đính kèm đúng; ca hộp thư chưa grant nhận `EMAIL_SENDER_NOT_GRANTED`, execution không có người dùng thiếu `allowInternalSystem` bị từ chối; gọi lặp cùng key trả `duplicate: true`.
   - Cơ cấu tổ chức: gọi route production bằng session người dùng thật; `org.me()` là nhân sự của người dùng sở hữu session, có thể khác nhân sự của Project key dùng ở local. Kiểm tra đủ các ca quyết định của contract: quản lý và người không phải quản lý (`isManagerOf` `true`/`false`), nhân viên/phó phòng/trưởng phòng, nhân sự nhiều phòng ban có và không có `departmentId`, nhân sự chưa có tài khoản với `accountOnly`, chuỗi rỗng hoặc có phòng trống; job, webhook và trigger do hệ thống ghi đọc được `org` khi policy có `allowInternalSystem: true`. Cần đổi phòng ban hoặc cấp quản lý của nhân sự test để có fixture thì làm qua `$user-permission` với xác nhận, snapshot và phục hồi sau test; chờ vài giây để thay đổi có hiệu lực.
8. Frontend theo dạng đã xác nhận, trong browser đã đăng nhập Workspace (CLI publish không đăng nhập browser; link frontend yêu cầu session Workspace, không phải trang công khai vô danh):
   - Single page app: mở `https://{WORKSPACE_DOMAIN}/{slugSlot}/index.html`, xác minh asset/load/refresh và luồng nghiệp vụ với API thật.
   - Custom component: thêm hoặc cập nhật item **Federation component** trên layout đích với đường dẫn `{slugSlot}/Components/<Tên>` khớp chữ hoa/thường với khóa expose; đặt qua `$object-layout` (mục "Đưa Federation component vào layout": component `fieldType: "federation_component"`, trường `federationUrl`) hoặc trình sửa layout, rồi view lại layout để xác minh. Mở form thật, chạy lại các ca ở bước 8, lưu và đọc lại record bằng `$object-record`; thay mọi URL localhost còn sót.
   - Federation Page: mở `https://{WORKSPACE_DOMAIN}/{APP_SLUG}/c{N}/{PATH}` với `N` từ `slugSlot` và App do người dùng chỉ định, đã xác minh qua `$app-menu-manager`; kiểm tra từng route, refresh, deep-link và quyền. Gắn vào menu ứng dụng bằng mục menu điều hướng tới trang trong Workspace với đường dẫn `/{APP_SLUG}/c{N}/{PATH}`; dùng API `$app-menu-manager` chỉ khi contract của skill đó mô tả loại hành động này, nếu không thì cấu hình trên giao diện rồi đọc lại menu.
   - Custom component và Federation Page: sau activate version mới, tải lại cứng, xác nhận không còn lỗi không tìm thấy remote module và asset tải từ `/{slugSlot}/assets/`.
9. Xác minh quyền bằng caller phù hợp: session từ Workspace API key không bảo đảm cùng user với Project key và không giả lập được user tùy ý; chưa test user thường thì báo đúng giới hạn. Quy tắc theo Super Admin/role: kiểm thử người gọi là Super Admin, người có role được phép và người không có role (persona test của `$user-permission` hoặc session của đúng user đó; session tạo từ Workspace API key mang role của user sở hữu key, user đó là Super Admin thì không chứng minh được ca bị từ chối); sau khi gán/gỡ role, HTTP invocation phản ánh ngay còn record trigger và background job có thể chậm khoảng một phút; phục hồi role test tạm. Object do backend quản lý: phối hợp sub-agent Security kiểm chứng action trực tiếp bị từ chối, action được phép (ví dụ View) vẫn đúng phạm vi, đường backend hợp lệ thực hiện được thao tác hệ thống; kiểm thử cả caller không được phép gọi nghiệp vụ. Thành công dưới `data.asSystem()` không chứng minh caller có quyền CRUD trực tiếp. Quy tắc theo cơ cấu tổ chức (chỉ quản lý của người tạo được duyệt, chỉ thành viên phòng ban được thao tác): kiểm thử bằng session của người thoả điều kiện và của người không thoả, không chỉ bằng user sở hữu Workspace API key. Hoàn tất các ca runtime còn thiếu từ bước 2A trước khi tuyên bố quyền đã được kiểm thử end-to-end.
10. Process: kiểm chứng instance và hiệu ứng nghiệp vụ theo `$process-creator`, phục hồi lịch và quyền test tạm. Module lỗi sau activate: ưu tiên activate lại version cũ còn hợp lệ trong phạm vi phục hồi và đọc lại xác nhận; rollback version không hoàn tác schema/record writes hay side effect.
11. Phục hồi cấu hình test tạm, xóa file session export do tác vụ tạo khi dùng xong; fixture Workspace xử lý theo skill dữ liệu và phạm vi đã được phép; báo rõ phần còn lại. Không tự xóa Project, schema hoặc Process để “cleanup”.

### 10. Bàn giao

- **Frontend:** project/version đang active, `slugSlot`, dạng đã xác nhận ở 1A, thư mục source và kết quả browser test. Single page app kèm link `/{slugSlot}/index.html`. Custom component kèm danh sách expose key, Object/layout đã gắn item Federation component và đường dẫn `{slugSlot}/Components/<Tên>`. Federation Page kèm URL từng route theo App, mục menu đã gắn và quyền hiển thị.
- **Backend:** lệnh `cogover-dev auth session --format curl --output ...` và **cURL hoàn chỉnh** tới production theo reference bàn giao, điền domain, slug, method, route, payload đã kiểm thử; không đưa cookie/token thật vào câu trả lời. Kèm response mong đợi và cách tạo lại session khi hết hạn. Có record trigger: bảng trigger (key, Object, timing, operations, `fields`/`changedFields`/`when`/`runWhen`/`writableFields`, mã lỗi và message), version đang áp dụng, bằng chứng kiểm thử bằng thay đổi bản ghi thật cho từng ca và cách gỡ/rollback (activate version khác hoặc deactivate). Có push message: liệt kê nơi gọi, loại message, người nhận/`exclude` và bằng chứng quan sát trên trình duyệt. Có background job: bảng job (key, tên, `timeoutMs`, `maxAttempts`, lịch cron/múi giờ hoặc nơi enqueue, quy tắc idempotency key, định dạng payload), version áp dụng, bằng chứng run (run ID, trạng thái, số lần thử, hiệu ứng đã đọc lại) và cách dừng/rollback (bỏ `schedule` hoặc activate version khác, deactivate). Có secret/credential: tên, loại, header/host được phép, `valueVersion`, ai giữ giá trị và cách xoay; không kèm giá trị. Có inbound access: inbound ID, name, mode, route prefix, URL gốc, hạn dùng, cách ký của bên gửi và cách rotate/revoke; không kèm inbound key hay HMAC secret. Có crypto: nơi gọi và thao tác (hash/HMAC, AES, RSA, `sign`/`verify`), thuật toán/mode/padding/định dạng chữ ký và encoding đã thống nhất với bên kia, tên secret chứa từng key, bên giữ private key/AES key và cách xoay (`secrets set` cùng tên, phối hợp thời điểm với bên kia); không kèm giá trị key. Có notification: nơi gọi, cách xác định người nhận (`org`, field record, danh sách cố định) và `exclude`, channel (record ID, tên kênh), link, người gửi, quy tắc `idempotencyKey` và bằng chứng quan sát trên danh sách thông báo. Có email: nơi gọi, hộp thư gửi (ID, địa chỉ, dùng chung hay `"actor"`) cùng mục `email` của policy đã duyệt cho version, người nhận hoặc field email, record ghi nhận timeline, file field đính kèm, quy tắc `idempotencyKey`, bằng chứng gửi (`requestId`, `delivery`, thư nhận hoặc hoạt động trên timeline). Có cơ cấu tổ chức: các hàm `org` dùng và quyết định nghiệp vụ dựa trên chúng (ai được duyệt, phòng ban nào được xét, `accountOnly`, `directOnly`), cách xử lý chuỗi rỗng/phòng trống/`truncated`, policy `allowInternalSystem` cho execution không có người dùng và các ca đã kiểm thử.
- **Cả hai:** hai mục trên cùng mapping frontend → backend, thư mục source và lệnh build/test/publish để bảo trì.
- **Kiểm thử:** báo cáo unit tests và line coverage của từng project theo bước 8, không gộp hai project thành một chỉ số; nêu rõ sub-agent phụ trách backend/frontend/Process đã thực hiện phần nào.
- **Security:** ma trận quyền, mapping bốn slot rule của từng Object mới và rules bổ sung nếu có, action chỉ backend được thực hiện, quy tắc nghiệp vụ theo Super Admin/role trong code (role ID, tên và thao tác áp dụng) và theo cơ cấu tổ chức (quản lý, phòng ban được xét), bằng chứng đọc lại/nghiệm thu của sub-agent Security, kết quả runtime cùng giới hạn còn lại. Phân biệt cấu hình đã xác minh với quyền đã kiểm thử end-to-end.
- Kèm phần schema đã tạo/tái sử dụng, workbook đã duyệt nếu có, Process/link/instance nếu có, policy/key ở mức metadata và bằng chứng kiểm thử. Báo `PASS`, `PARTIAL`, `FAIL` hoặc `BLOCKED` theo kết quả quan sát; liệt kê thiếu quyền/dependency/fixture còn lại. Chỉ gọi hoàn tất khi local và Workspace test đáp ứng tiêu chí của bài toán.
