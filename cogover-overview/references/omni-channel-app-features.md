# Omni Channel App Feature List

Phiên bản app: 1.6.8 (17/08/2026). Cột **Object liên quan** ghi slug chuẩn của Object lưu trữ hoặc cung cấp dữ liệu cho tính năng.

| Nhóm | Tính năng | Mô tả ngắn | Object liên quan |
|---|---|---|---|
| Kênh chat | Hợp nhất hội thoại đa kênh | Quản lý tập trung hội thoại từ chat nội bộ, Live Chat, Facebook Messenger, Facebook Comment, Zalo OA, Zalo Group, WhatsApp Business, TikTok Business Messaging, TikTok Shop và AI Chat trên cùng một giao diện. | `conversation`, `conversation_participant`, `message`, `visitor`, `page` |
| Kênh chat | Quản lý trạng thái kết nối kênh | Sử dụng thông tin kết nối của từng kênh và ngăn gửi tin khi kênh đã ngừng hoạt động hoặc chưa được cấu hình đầy đủ. | `page`, `conversation`, `visitor` |
| Hội thoại | Tạo hội thoại cá nhân và nhóm | Tạo hội thoại riêng hoặc theo nhóm, tránh tạo trùng hội thoại giữa cùng một nhóm người; hỗ trợ tối đa 200 thành viên và tối đa 1.000 thành viên đối với Zalo Group. | `conversation`, `conversation_participant`, `message`, `visitor`, `personnel`, `ai_agent` |
| Hội thoại | Quản lý thành viên hội thoại | Thêm hoặc xoá nhân viên, khách hàng, tài khoản đại diện của kênh và AI Agent; quản lý vai trò quản trị viên, thành viên và lịch sử tham gia hội thoại. | `conversation`, `conversation_participant`, `visitor`, `personnel`, `ai_agent` |
| Hội thoại | Liên kết hồ sơ khách hàng | Liên kết một hội thoại với một hoặc nhiều người liên hệ hoặc khách hàng tiềm năng để nhân viên có đầy đủ thông tin trong quá trình trao đổi. | `conversation`, `visitor`, `contact`, `lead` |
| Hội thoại | Cập nhật thông tin và trạng thái | Cập nhật tên, ảnh đại diện, người phụ trách và trạng thái của hội thoại trong suốt quá trình tiếp nhận, phân phối, xử lý và kết thúc. | `conversation`, `conversation_participant`, `message` |
| Hội thoại | Kết thúc hội thoại | Kết thúc một hội thoại khi yêu cầu của khách hàng đã được xử lý và thông báo thay đổi đến các thành viên liên quan. | `conversation`, `conversation_participant`, `message` |
| Phân công | Chuyển người phụ trách | Chuyển hội thoại đang xử lý sang một nhân viên khác, có thể giữ hoặc loại người phụ trách cũ khỏi hội thoại và ghi nhận lịch sử chuyển giao. | `conversation`, `conversation_participant`, `personnel`, `message` |
| Phân công | Chuyển lại hàng đợi phân phối tự động | Đưa hội thoại từ người phụ trách hiện tại trở lại hàng đợi để hệ thống tự chọn nhân viên phù hợp theo quy tắc đã cấu hình. | `conversation`, `conversation_participant`, `omni_router`, `personnel`, `message` |
| Phân công | Tiếp nhận hội thoại bằng AI Agent | Tự động đưa AI Agent vào hội thoại Live Chat khi kênh được cấu hình để AI tiếp nhận và hỗ trợ khách hàng. | `page`, `omni_router`, `ai_agent`, `conversation`, `conversation_participant`, `message` |
| Danh sách | Tải và lọc danh sách hội thoại | Lọc hội thoại theo kênh, trang, thời gian, nhãn, người phụ trách, người tạo và trạng thái; hỗ trợ xem lại các hội thoại đã được ẩn khỏi danh sách cá nhân. | `conversation`, `conversation_participant`, `page`, `personnel` |
| Danh sách | Xem chi tiết hội thoại | Mở một hội thoại và hiển thị đầy đủ thông tin, thành viên cùng trạng thái hiện tại theo quyền truy cập của người dùng. | `conversation`, `conversation_participant` |
| Phân quyền | Phân quyền xem hội thoại theo vai trò và kênh | Cho phép giới hạn nhân viên chỉ xem hội thoại của mình, của mình hoặc chưa được phân công, của đội nhóm hoặc toàn bộ hội thoại; phạm vi có thể áp dụng cho tất cả hoặc từng kênh cụ thể. | `role`, `user_role`, `permission`, `permission_value`, `page`, `personnel`, `department`, `department_personnel`, `conversation` |
| Tổ chức | Gắn và gỡ nhãn hội thoại | Gắn nhiều nhãn đã được cấu hình để phân loại hội thoại, đồng thời cho phép gỡ nhãn khi không còn phù hợp. | `conversation`, `object_type`, `object_field`, `object_field_multi_option` |
| Đã đọc | Đếm hội thoại chưa đọc | Hiển thị tổng số hội thoại chưa đọc theo quyền truy cập của từng nhân viên trên cả chat nội bộ và các kênh chăm sóc khách hàng. | `conversation`, `conversation_participant`, `role`, `permission`, `page` |
| Đã đọc | Đánh dấu chưa đọc thủ công | Cho phép nhân viên đánh dấu một hội thoại là chưa đọc để quay lại xử lý sau. | `conversation`, `conversation_participant` |
| Lịch sử | Xoá lịch sử và ẩn hội thoại cá nhân | Cho phép một thành viên ẩn lịch sử cũ hoặc xoá hội thoại khỏi danh sách của riêng mình mà không ảnh hưởng đến dữ liệu của thành viên khác. | `conversation`, `conversation_participant`, `message` |
| Lịch sử | Đóng hội thoại cho tất cả thành viên | Tự động đóng và ẩn hội thoại đối với toàn bộ thành viên trong các trường hợp kênh bên ngoài không còn tồn tại, chẳng hạn một nhóm Zalo đã bị giải tán. | `conversation`, `conversation_participant` |
| Tin nhắn | Gửi tin nhắn văn bản | Gửi nội dung văn bản trong hội thoại và hiển thị tiến trình xử lý khi AI Agent đang chuẩn bị câu trả lời. | `message`, `conversation`, `conversation_participant` |
| Tin nhắn | Trả lời hoặc trích dẫn tin nhắn | Trả lời trực tiếp một tin nhắn trước đó và duy trì liên kết với nội dung được trích dẫn trên những kênh có hỗ trợ. | `message`, `message_by_id`, `message_by_social_id`, `conversation` |
| Tin nhắn | Gửi tệp đính kèm đa định dạng | Hỗ trợ ảnh, GIF, âm thanh, video, file, liên kết, vị trí, sticker và danh bạ; tự kiểm tra định dạng, dung lượng và số lượng cho phép của từng kênh. | `message`, `attachment`, `attachment_by_id`, `conversation`, `page` |
| Tin nhắn | Gửi tin trên từng nền tảng | Tự chuyển đổi nội dung và tệp đính kèm sang định dạng phù hợp của Facebook, Zalo, WhatsApp và TikTok, giúp nhân viên trả lời nhiều kênh từ một nơi. | `message`, `attachment`, `conversation`, `page`, `visitor` |
| Tin nhắn | Xem lịch sử và mở lại tin nhắn | Xem các tin nhắn cũ theo từng trang, tôn trọng lịch sử đã ẩn và quyền xem của người dùng; đồng thời hỗ trợ mở nhanh các tin nhắn cụ thể. | `message`, `message_by_created`, `message_by_id`, `conversation`, `conversation_participant` |
| Tin nhắn | Trạng thái đã nhận, đã xem và số chưa đọc | Theo dõi tin nhắn đã được nhận hoặc đã xem, cập nhật số lượng chưa đọc và tự đồng bộ trạng thái phù hợp cho Zalo Group. | `conversation`, `conversation_participant`, `message` |
| Tin nhắn | Xoá tin nhắn theo người dùng | Cho phép một thành viên ẩn tối đa 100 tin nhắn mỗi lần đối với riêng mình mà không làm mất nội dung của những thành viên khác. | `message`, `message_by_id`, `conversation`, `conversation_participant` |
| Tin nhắn | Ghim và bỏ ghim tin nhắn | Ghim tối đa 10 tin nhắn quan trọng trong một hội thoại và thông báo thay đổi đến các thành viên. | `conversation`, `message`, `message_by_id`, `conversation_participant` |
| Tương tác | Thả và gỡ cảm xúc | Cho phép người dùng bày tỏ cảm xúc với tin nhắn; danh sách cảm xúc được điều chỉnh theo khả năng hỗ trợ của từng kênh. | `reaction`, `message`, `conversation`, `conversation_participant`, `page` |
| Tương tác | Hiển thị trạng thái đang nhập | Hiển thị khi thành viên hoặc AI Agent đang soạn câu trả lời trên các kênh được hỗ trợ. | `conversation`, `conversation_participant`, `page` |
| Tìm kiếm | Tìm hội thoại và tin nhắn | Tìm theo tên hội thoại hoặc nội dung tin nhắn trong toàn bộ phạm vi được phép xem hoặc trong một hội thoại cụ thể; hỗ trợ lọc theo thời gian và xem thêm kết quả. | `conversation`, `message`, `role`, `permission`, `page` |
| Facebook | Quản lý bình luận Facebook | Trả lời đúng bình luận, ẩn hoặc hiện lại bình luận của khách hàng và xoá bình luận trên Facebook ngay từ màn hình hội thoại. | `conversation`, `message`, `page`, `visitor` |
| Facebook | Xem bài viết gốc của hội thoại bình luận | Xem nội dung bài viết Facebook, người đăng, thời gian, đường dẫn và tệp đính kèm để hiểu đầy đủ ngữ cảnh trao đổi. | `facebook_post`, `facebook_post_attachment`, `conversation` |
| Facebook | Khởi tạo Messenger chat từ bình luận | Mở cuộc trò chuyện Messenger riêng với người đã bình luận, giữ liên kết đến hội thoại gốc và hồ sơ khách hàng; hỗ trợ gửi tin nhắn riêng trong thời hạn Facebook cho phép. | `conversation`, `conversation_participant`, `message`, `visitor`, `page`, `contact`, `lead` |
| Facebook | Hỗ trợ nhắn tin ngoài khung thời gian thông thường | Hỗ trợ nhân viên tiếp tục liên hệ với khách hàng từ hội thoại phát sinh qua bình luận khi cuộc trò chuyện đã nằm ngoài khung nhắn tin Messenger thông thường, trong phạm vi chính sách của Facebook. | `conversation`, `message`, `attachment`, `visitor`, `page` |
| Tệp | Xem trước tệp đính kèm an toàn | Tạo đường dẫn xem trước có thời hạn cho tệp thuộc hội thoại, giúp hạn chế chia sẻ ngoài phạm vi được phép. | `attachment_by_id`, `conversation` |
| Thông báo | Cập nhật hội thoại theo thời gian thực | Tự động cập nhật tin nhắn, trạng thái hội thoại, trạng thái gửi và trạng thái đang nhập cho những người đang theo dõi hội thoại. | `conversation`, `conversation_participant`, `message`, `page`, `personnel`, `ai_agent` |
| Thông báo | Thông báo trên web và thiết bị di động | Gửi thông báo khi có tin nhắn mới đến những người liên quan hoặc có quyền theo dõi hội thoại, đồng thời tránh gửi lại cho chính người gửi. | `page`, `conversation`, `conversation_participant`, `message`, `personnel` |
| Thông báo | Quy tắc chọn người nhận thông báo | Thiết lập người nhận thông báo theo từng kênh dựa trên nhân viên, vai trò, chức vụ, phòng ban hoặc toàn bộ cây phòng ban. | `chat_notification_recipient_rules`, `page`, `personnel`, `role`, `position`, `department`, `department_personnel` |
